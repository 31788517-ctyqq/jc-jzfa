# 量化数据排行榜 & 场次PK — 开发文档 V1.0

> 生成日期：2026-05-27  
> 对应产品文档：`sucai/足球雷达排行榜_实力PK_产品文档.md`

---

## 一、项目现状总览

### 1.1 文件结构

| 文件 | 职责 | 当前完成度 |
| :--- | :--- | :--- |
| `preview/index.html` (2159~2178行) | HTML结构：3个tab + 表格容器 + PK弹窗 | ✅ 100% |
| `preview/js/pages/quant-rank.js` | 排行榜：数据获取、合并、渲染、排序 | ⚠️ 60%（仅实现实力tab） |
| `preview/js/pages/match-pk.js` | PK弹窗：多场对比、结果卡 | ⚠️ 40%（仅实现进球PK） |
| `preview/js/main.js` | 全局路由、事件连线 | ⚠️ 需微调 startPK 连线 |
| `server/index.js` `'ranking-list'` | 提供比赛列表 | ✅ 已有 |
| `server/index.js` `'gongshoudao'` | 提供功守道量化数据 | ✅ 已有 |

### 1.2 当前问题

1. **三个 tab 共用一套表格列** — `switchQuantTab()` 切换后仅调用 `renderTable()`，列结构不变
2. **PK 弹窗不考虑 tab** — `startPK()` 不传 `currentTab`，`openPKMulti()` 无 tab 参数
3. **进球 tab 和热点 tab 仅有占位 UI**，无实际数据渲染

---

## 二、数据结构分析

### 2.1 ranking-list API 返回（用于 quant-rank）

```js
// ranking[i] 核心字段：
{
  matchId, num, homeName, visitName, leagueName, date, matchStatus, rank
}
```

### 2.2 gongshoudao API 返回（用于 mergeItem）

```js
// gs 核心字段：
{
  // 实力相关
  totalAdvantage, totalAdvantageValue,     // 综合实力
  attackAdvantageValue, defenseAdvantageValue,  // 攻守实力
  crossWin, crossDraw, crossLose, crossRq,      // 胜平负交叉
  attackPattern,                          // 有值则表示有效
  
  // 进球相关
  goalDiffHome, goalDiffAway,             // 主客进球期望
  totalGoalsExpect, totalGoalsValue,      // 总进球期望
  goalRange,                              // 进球范围
  
  // 净胜球
  homeWinExpect, homeWinValue,           // 主胜期望
  totalAdvantage2, totalAdvantage2Value, // 综合实力2
}
```

### 2.3 热点数据可用性

**热点排行榜所需的数据字段**（来自产品文档）：

| 字段 | 数据来源 | 前端可用性 |
| :--- | :--- | :--- |
| 让球数 (rq) | odds_history JSON | ❌ 无 API 端点 |
| 关注热度 (hotFocusNum) | 500.com 爬虫 | ❌ 无 API 端点 |
| 冷热指数 (R_heat) | 前端算法计算 | 依赖上面数据 |
| 主队特征 / 客队特征 | 欧指概率解读 | ❌ 无 API 端点 |
| 静态实力差 | gongshoudao | ✅ totalAdvantageValue |
| 亚指临盘 | odds_history | ❌ 无 API 端点 |

**结论**：热点 tab 数据需要新建后端起拉取 API，或复用 `gongshoudao` + `odds_history` 的联合查询。  
**当前策略**：热点 tab 先渲染占位状态 "数据接入中"，待后端数据就绪后对接。

---

## 三、实施计划

### 3.1 总览流程图

```
quant-rank.js
├── mergeItem() 扩展
│   ├── goal 维度：bigBallRatio, attDefGoal, strengthGoal, headToHeadGoal, breakArmor, totalSum
│   └── hot 维度：rq, hotFocusNum, heatIndex, homeFeature, guestFeature, staticDiff, oddsLive
├── renderTable() 按 tab 动态切换
│   ├── power 列：[对阵 | 总排序 | 净胜球 | 胜平负交叉 | 综合实力 | 攻守实力]
│   ├── goal 列：[对阵 | 合计 | 大球比例 | 攻防进球 | 实力进球 | 交锋进球 | 破甲和]
│   └── hot 列：[对阵 | 让球数 | 关注热度 | 冷热指数 | 主队特征 | 客队特征 | 静态实力差 | 亚指临盘]
├── sortBy() 扩展排序键
└── startPK() → 传入 currentTab
        │
        ▼
match-pk.js
├── openPKMulti(pickedList, tab) ← 新增 tab 参数
├── renderPKPower() ← 新增：实力维度PK
├── renderPKGoal()  ← 现有进球PK逻辑
└── renderPKHot()   ← 新增：热度维度PK
```

### 3.2 改动清单

#### 改动 1：`preview/js/pages/quant-rank.js`

##### 1a. `mergeItem()` 扩展字段（约107~127行）

新增 goal 和 hot 维度的数据提取：

```js
function mergeItem(item, gs) {
  var base = {
    matchId: item.matchId,
    num: item.num || '',
    homeName: item.homeName || '',
    visitName: item.visitName || '',
    leagueName: item.leagueName || '',
    date: item.date || '',
    matchStatus: item.matchStatus || 0,
    rank: item.rank || 99,
    // 实力维度
    totalAdvantage: gs.totalAdvantage || '-',
    totalAdvantageValue: gs.totalAdvantageValue || 0,
    goalDiff: gs.goalDiffHome || '-',
    crossWin: gs.crossWin !== undefined ? gs.crossWin : '-',
    crossDraw: gs.crossDraw !== undefined ? gs.crossDraw : '-',
    crossLose: gs.crossLose !== undefined ? gs.crossLose : '-',
    crossRq: gs.crossRq,
    attackAdvantageValue: gs.attackAdvantageValue || 0,
    defenseAdvantageValue: gs.defenseAdvantageValue || 0,
    hasGS: !!(gs.attackPattern),

    // ═══ 进球维度（新增） ═══
    // 大球比例 = 综合主客及交锋大球比例（从 totalGoalsExpect 映射）
    bigBallRatio: gs.totalGoalsExpect || 0,
    // 攻防进球 = goalDiffHome + goalDiffAway（射门还原预测总进球）
    attDefGoal: (parseFloat(gs.goalDiffHome) || 0) + (parseFloat(gs.goalDiffAway) || 0),
    // 实力进球 = totalAdvantageValue 映射 -> 主客实力加权进球预期
    strengthGoal: gs.totalGoalsValue || 0,
    // 交锋进球 = 基于 cross 数据推导（用 crossWin/crossLose 做简化的对冲进球预期）
    headToHeadGoal: (((gs.crossWin || 0) - (gs.crossLose || 0)) / 10) + 2.5,
    // 破甲和 = (attackAdvantageValue + defenseAdvantageValue) / 100
    breakArmor: ((gs.attackAdvantageValue || 0) + (gs.defenseAdvantageValue || 0)) / 100,
    // 合计 = 各维度绝对值之和
    totalSum: computeGoalTotalSum(gs),

    // ═══ 热点维度（新增） ═══
    rq: gs.crossRq !== undefined ? gs.crossRq : '-',
    hotFocusNum: '-',         // 占位，等后端数据
    heatIndex: '-',           // 占位，等后端数据
    homeFeature: '-',         // 占位
    guestFeature: '-',        // 占位
    staticDiff: gs.totalAdvantageValue || 0,
    oddsLive: '-',            // 占位
  };
  return base;
}

// 辅助：计算合计
function computeGoalTotalSum(gs) {
  var b = Math.abs(parseFloat(gs.totalGoalsExpect) || 0);
  var a = Math.abs((parseFloat(gs.goalDiffHome) || 0) + (parseFloat(gs.goalDiffAway) || 0));
  var s = Math.abs(parseFloat(gs.totalGoalsValue) || 0);
  var h = Math.abs(((gs.crossWin || 0) - (gs.crossLose || 0)) / 10 + 2.5);
  var r = Math.abs(((gs.attackAdvantageValue || 0) + (gs.defenseAdvantageValue || 0)) / 100);
  return parseFloat((b + a + s + h + r).toFixed(4));
}
```

##### 1b. `renderTable()` 重构（约130~173行）

核心改动：根据 `currentTab` 确定列定义，并按 `currentTab` 决定每行的单元格渲染。

```js
function renderTable() {
  var wrap = document.getElementById('quantTableWrap');
  if (!wrap) return;

  var sorted = allData.slice().sort(/* 排序逻辑不变 */);

  // 根据 currentTab 确定列定义
  var cols;
  if (currentTab === 'power') {
    cols = [
      { key: 'match', label: '对阵', sortable: false, cls: 'th-match' },
      { key: 'rank', label: '总排序', sortable: true, cls: 'th-rank' },
      { key: 'goalDiff', label: '净胜球', sortable: true, cls: 'th-gd' },
      { key: 'cross', label: '胜平负交叉', sortable: false, cls: 'th-cross' },
      { key: 'power', label: '综合实力', sortable: true, cls: 'th-power' },
      { key: 'ad', label: '攻守实力', sortable: false, cls: 'th-ad' }
    ];
  } else if (currentTab === 'goal') {
    cols = [
      { key: 'match', label: '对阵', sortable: false, cls: 'th-match' },
      { key: 'totalSum', label: '合计', sortable: true, cls: 'th-sum' },
      { key: 'bigBallRatio', label: '大球比例', sortable: true, cls: 'th-big' },
      { key: 'attDefGoal', label: '攻防进球', sortable: true, cls: 'th-ag' },
      { key: 'strengthGoal', label: '实力进球', sortable: true, cls: 'th-sg' },
      { key: 'headToHeadGoal', label: '交锋进球', sortable: true, cls: 'th-hg' },
      { key: 'breakArmor', label: '破甲和', sortable: true, cls: 'th-ba' }
    ];
  } else {
    cols = [
      { key: 'match', label: '对阵', sortable: false, cls: 'th-match' },
      { key: 'rq', label: '让球数', sortable: false, cls: 'th-rq' },
      { key: 'hotFocusNum', label: '关注热度', sortable: true, cls: 'th-hot' },
      { key: 'heatIndex', label: '冷热指数', sortable: true, cls: 'th-heat' },
      { key: 'homeFeature', label: '主队特征', sortable: false, cls: 'th-hf' },
      { key: 'guestFeature', label: '客队特征', sortable: false, cls: 'th-gf' },
      { key: 'staticDiff', label: '静态实力差', sortable: true, cls: 'th-sd' },
      { key: 'oddsLive', label: '亚指临盘', sortable: false, cls: 'th-ol' }
    ];
  }

  // 表头
  var h = '<table class="quant-table"><thead><tr><th class="th-chk"></th>';
  cols.forEach(function (c) {
    var ad = sortKey === c.key ? (sortAsc ? ' sort-asc' : ' sort-desc') : '';
    h += '<th class="' + c.cls + (c.sortable ? ' sortable' + ad : '') + '" onclick="' + (c.sortable ? 'sortBy(\'' + c.key + '\')' : '') + '">' + c.label + '</th>';
  });
  h += '</tr></thead><tbody>';

  // 数据行
  sorted.forEach(function (item) {
    var p = !!pickedIds[item.matchId];
    h += '<tr id="qr-' + item.matchId + '" class="' + (p ? 'picked' : '') + '">' +
      '<td><input type="checkbox" class="q-chk" ' + (p ? 'checked' : '') + ' onclick="togglePick(event,\'' + item.matchId + '\')"/></td>' +
      tdMatch(item);  // 对阵列（三tab共用）
    
    // 按 currentTab 拼不同列的单元格
    if (currentTab === 'power') {
      h += tdRank(item.rank);
      h += tdNum(item, 'goalDiff');
      h += tdCross(item);
      h += tdNum(item, 'power');
      h += tdAd(item);
    } else if (currentTab === 'goal') {
      h += tdGoalCell(item, 'totalSum', 2);
      h += tdGoalCell(item, 'bigBallRatio', 4);
      h += tdGoalCell(item, 'attDefGoal', 2);
      h += tdGoalCell(item, 'strengthGoal', 2);
      h += tdGoalCell(item, 'headToHeadGoal', 2);
      h += tdGoalCell(item, 'breakArmor', 2);
    } else {
      h += tdHotCell(item, 'rq');
      h += tdHotCell(item, 'hotFocusNum');
      h += tdHotCell(item, 'heatIndex');
      h += tdHotCell(item, 'homeFeature');
      h += tdHotCell(item, 'guestFeature');
      h += tdHotCell(item, 'staticDiff');
      h += tdHotCell(item, 'oddsLive');
    }
    h += '</tr>';
  });

  h += '</tbody></table>';
  wrap.innerHTML = h;
  updatePkBar();
}
```

##### 1c. 新增进球维度单元格函数

```js
// 进球 tab 单元格
function tdGoalCell(item, key, digits) {
  var v = item[key];
  if (v === '-' || v === undefined || v === null) {
    return '<td><span class="q-cell-num" style="color:var(--text4)">-</span></td>';
  }
  var n = parseFloat(v);
  if (isNaN(n)) return '<td><span class="q-cell-num">' + v + '</span></td>';
  
  var formatted;
  if (key === 'bigBallRatio') {
    formatted = n.toFixed(1) + '%';
  } else {
    formatted = (n >= 0 ? '+' : '') + n.toFixed(digits);
  }
  var cls = n > 0 ? 'pos' : n < 0 ? 'neg' : '';
  return '<td><span class="q-cell-num ' + cls + '">' + formatted + '</span></td>';
}

// 热点 tab 单元格
function tdHotCell(item, key) {
  var v = item[key];
  if (v === '-' || v === undefined || v === null) {
    return '<td><span class="q-cell-num" style="color:var(--text4);font-size:10px">数据接入中</span></td>';
  }
  
  if (key === 'heatIndex') {
    var n = parseFloat(v);
    if (isNaN(n)) return '<td><span class="q-cell-num">' + v + '</span></td>';
    var icon = n > 1.2 ? '🔥' : n < 0.8 ? '🧊' : '🎯';
    var cls = n > 1.2 ? 'neg' : n < 0.8 ? 'cool' : 'pos';
    return '<td><span class="q-cell-num ' + cls + '">' + n.toFixed(2) + ' ' + icon + '</span></td>';
  }
  
  var n = parseFloat(v);
  if (isNaN(n)) return '<td><span class="q-cell-num">' + v + '</span></td>';
  if (key === 'hotFocusNum' && n > 1000) v = (n / 10000).toFixed(1) + '万';
  return '<td><span class="q-cell-num">' + v + '</span></td>';
}
```

##### 1d. `sortBy()` 扩展排序键

```js
function getSortVal(item, key) {
  switch (key) {
    // 实力
    case 'rank':       return item.rank || 99;
    case 'goalDiff':   var p = String(item.goalDiff).split('/'); return parseFloat(p[0]) || 0;
    case 'power':      return item.totalAdvantageValue || 0;
    case 'ad':         return (item.attackAdvantageValue || 0) + (item.defenseAdvantageValue || 0);
    // 进球
    case 'totalSum':       return parseFloat(item.totalSum) || 0;
    case 'bigBallRatio':   return parseFloat(item.bigBallRatio) || 0;
    case 'attDefGoal':     return parseFloat(item.attDefGoal) || 0;
    case 'strengthGoal':   return parseFloat(item.strengthGoal) || 0;
    case 'headToHeadGoal': return parseFloat(item.headToHeadGoal) || 0;
    case 'breakArmor':     return parseFloat(item.breakArmor) || 0;
    // 热点
    case 'hotFocusNum': return parseFloat(item.hotFocusNum) || 0;
    case 'heatIndex':   return parseFloat(item.heatIndex) || 0;
    case 'staticDiff':  return parseFloat(item.staticDiff) || 0;
    default: return 0;
  }
}
```

##### 1e. `startPK()` 传递 currentTab

```js
export function startPK() {
  var picked = allData.filter(function (item) { return pickedIds[item.matchId]; });
  if (picked.length < 2) return;
  if (window.openPKMulti) {
    window.openPKMulti(picked, currentTab);  // ← 新增第二个参数
    clearPicks();
  }
}
```

---

#### 改动 2：`preview/js/pages/match-pk.js`

##### 2a. `openPKMulti()` 增加 tab 参数（约8行）

```js
export function openPKMulti(pickedList, tab) {
  var overlay = document.getElementById('pkOverlay');
  if (!overlay || pickedList.length < 2) return;
  overlay.classList.add('active');
  document.body.style.overflow = 'hidden';

  var modal = document.getElementById('pkModal');
  if (!modal) return;
  modal.innerHTML = '<div style="text-align:center;padding:80px 20px;color:var(--cyan)">' +
    '<div style="font-size:36px;margin-bottom:12px">⚔️</div>' +
    '<div style="font-size:14px;font-weight:600">PK数据分析中...</div></div>';

  var promises = pickedList.map(function (item) {
    if (item.hasGS && item.totalAdvantage) return Promise.resolve(item);
    return api('gongshoudao', { matchId: item.matchId }).then(function (gs) {
      return Object.assign({}, item, buildGSFields(gs));
    }).catch(function () { return item; });
  });

  Promise.all(promises).then(function (fullList) {
    var t = tab || 'power';  // 默认实力
    if (t === 'power') renderPKPower(modal, fullList);
    else if (t === 'goal') renderPKGoal(modal, fullList);
    else renderPKHot(modal, fullList);
  });
}
```

##### 2b. 新增 `renderPKPower()` — 实力维度PK

对应产品文档的实力雷达指标：总排序、净胜球、胜平负交叉、综合实力、攻守实力。

```js
function renderPKPower(modal, list) {
  var n = list.length;
  var html =
    '<div class="pk2-header">' +
      '<span class="pk2-title">⚔️ 实力PK对比</span>' +
      '<span class="pk2-close" onclick="closePK()">✕</span>' +
    '</div>';

  html += '<div class="pk2-section-label">实力维度指标对比</div>';
  html += '<div class="pk2-table-wrap"><table class="pk2-table">';
  
  // 表头
  html += '<thead><tr><th class="pk2-th-name">对阵</th>' +
    '<th class="pk2-th-num">总排序</th>' +
    '<th class="pk2-th-num">净胜球<br>占比</th>' +
    '<th class="pk2-th-num">胜平负<br>占比</th>' +
    '<th class="pk2-th-num">综合实力<br>占比</th>' +
    '<th class="pk2-th-num">攻守实力<br>占比</th>' +
    '</tr></thead><tbody>';

  // 列合计
  var colSums = { gd: 0, cross: 0, power: 0, ad: 0 };
  list.forEach(function (item) {
    colSums.gd    += Math.abs(parseFloat(String(item.goalDiff).split('/')[0]) || 0);
    colSums.cross += Math.abs((item.crossWin || 0) - (item.crossLose || 0));
    colSums.power += Math.abs(item.totalAdvantageValue - 50);
    colSums.ad    += Math.abs(item.attackAdvantageValue + item.defenseAdvantageValue - 100);
  });

  list.forEach(function (item, i) {
    var gd    = parseFloat(String(item.goalDiff).split('/')[0]) || 0;
    var cross = (item.crossWin || 0) - (item.crossLose || 0);
    var power = item.totalAdvantageValue - 50;
    var ad    = item.attackAdvantageValue + item.defenseAdvantageValue - 100;
    
    html += '<tr class="pk2-row">' +
      '<td class="pk2-td-name">' +
        '<span class="pk2-row-idx">' + (i + 1) + '</span>' +
        '<span class="pk2-team-name">' + esc(item.homeName) + ' vs ' + esc(item.visitName) + '</span>' +
        '<div class="pk2-team-num">' + esc(item.num) + '</div></td>' +
      '<td class="pk2-td-num">' + (item.rank || '-') + '</td>' +
      tdPctBar(gd,    colSums.gd) +
      tdPctBar(cross, colSums.cross) +
      tdPctBar(power, colSums.power) +
      tdPctBar(ad,    colSums.ad) +
      '</tr>';
  });

  // 合计行
  html += '<tr class="pk2-row-total"><td class="pk2-td-name"><b>列合计</b></td>' +
    '<td class="pk2-td-num"></td>' +
    tdPct(colSums.gd, colSums.gd) +
    tdPct(colSums.cross, colSums.cross) +
    tdPct(colSums.power, colSums.power) +
    tdPct(colSums.ad, colSums.ad) +
    '</tr>';
  html += '</tbody></table></div>';

  // PK结果卡
  html += '<div class="pk2-section-label">🏆 实力PK结果</div>';
  html += renderPowerPKCard(list);
  html += '<div class="pk2-footer"><button class="pk2-done-btn" onclick="closePK()">完成</button></div>';

  modal.innerHTML = html;
}

function renderPowerPKCard(list) {
  if (list.length === 2) return renderPowerDuel(list[0], list[1]);
  
  var html = '';
  for (var i = 0; i < list.length - 1; i++) {
    for (var j = i + 1; j < list.length; j++) {
      html += '<div style="font-size:10px;color:var(--text3);padding:8px 16px 2px">对决 ' + (i+1) + ' vs ' + (j+1) + '</div>';
      html += renderPowerDuel(list[i], list[j]);
    }
  }
  return html;
}

function renderPowerDuel(a, b) {
  var dims = [
    { label: '总排序PK',    va: a.rank || 99, vb: b.rank || 99, lowerWins: true },
    { label: '净胜球PK',    va: parseFloat(String(a.goalDiff).split('/')[0]) || 0, vb: parseFloat(String(b.goalDiff).split('/')[0]) || 0 },
    { label: '胜平负PK',    va: (a.crossWin || 0) - (a.crossLose || 0), vb: (b.crossWin || 0) - (b.crossLose || 0) },
    { label: '综合实力PK',  va: a.totalAdvantageValue || 0, vb: b.totalAdvantageValue || 0 },
    { label: '攻守实力PK',  va: a.attackAdvantageValue + a.defenseAdvantageValue, vb: b.attackAdvantageValue + b.defenseAdvantageValue }
  ];

  var aWins = 0, bWins = 0;
  var rows = dims.map(function (d) {
    var result;
    if (d.lowerWins) {
      result = d.va < d.vb ? 'a' : d.vb < d.va ? 'b' : 'draw';
    } else {
      result = d.va > d.vb ? 'a' : d.vb > d.va ? 'b' : 'draw';
    }
    if (result === 'a') aWins++; else if (result === 'b') bWins++;
    
    return '<div class="pk2-result-row">' +
      '<span class="pk2-result-icon ' + (result === 'a' ? 'pk2-win' : result === 'b' ? 'pk2-lose' : '') + '">' +
        (result === 'a' ? '👑' : '') + '</span>' +
      '<span class="pk2-result-label">' + d.label + '</span>' +
      '<span class="pk2-result-values">' +
        '<span class="' + (result === 'a' ? 'pk2-win' : '') + '">' + esc(a.homeName) + ' (' + d.va + ')</span>' +
        ' vs ' +
        '<span class="' + (result === 'b' ? 'pk2-win' : '') + '">' + esc(b.homeName) + ' (' + d.vb + ')</span>' +
      '</span>' +
      '<span class="pk2-result-arrow ' + (result === 'a' ? 'pk2-win' : result === 'b' ? 'pk2-lose' : '') + '">' +
        (result === 'a' ? esc(a.homeName) + ' 胜' : result === 'b' ? esc(b.homeName) + ' 胜' : '平') +
      '</span>' +
    '</div>';
  }).join('');

  return '<div class="pk2-card">' +
    '<div class="pk2-card-head">' +
      '<div class="pk2-card-team">' + esc(a.homeName) + '<span style="font-size:10px;color:var(--text3)"> ' + esc(a.num) + '</span></div>' +
      '<span class="pk2-card-vs">VS</span>' +
      '<div class="pk2-card-team">' + esc(b.homeName) + '<span style="font-size:10px;color:var(--text3)"> ' + esc(b.num) + '</span></div>' +
    '</div>' +
    '<div class="pk2-card-body">' + rows + '</div>' +
    '<div class="pk2-card-summary">🏆 总PK：' +
      '<b style="color:var(--cyan)">' + esc(a.homeName) + ' (' + aWins + '胜)</b> ' +
      (aWins > bWins ? '👑' : aWins === bWins ? '⚖️ 平局' : '') +
      (bWins > aWins ? ' <b style="color:#f97316">' + esc(b.homeName) + ' (' + bWins + '胜)</b> 👑' : '') +
    '</div></div>';
}

// 带进度条的百分比单元格
function tdPctBar(val, sum) {
  var absV = Math.abs(val);
  var pct = sum > 0 ? Math.round(absV / sum * 100) : 0;
  var sign = val >= 0 ? '+' : '';
  var cls = val >= 0 ? 'pk2-pct-pos' : 'pk2-pct-neg';
  return '<td class="pk2-td-num"><div class="pk2-pct-bar-wrap"><div class="pk2-pct-bar ' + cls + '" style="width:' + pct + '%"></div></div>' +
    '<span class="' + cls + '">' + sign + absV.toFixed(2) + '<br>' + pct + '%</span></td>';
}
```

##### 2c. 现有进球PK逻辑 → `renderPKGoal()`

将现有 `renderPK()` 函数重命名为 `renderPKGoal()`（内容保持不变，仅改函数名），因为它已完整实现了进球维度的PK。

##### 2d. 新增 `renderPKHot()` — 热度维度PK

```js
function renderPKHot(modal, list) {
  var n = list.length;
  var html =
    '<div class="pk2-header">' +
      '<span class="pk2-title">🔥 热度PK对比</span>' +
      '<span class="pk2-close" onclick="closePK()">✕</span>' +
    '</div>';

  // 检查热度数据是否可用
  var hasHotData = list.some(function(item) { return item.hotFocusNum !== '-' && item.hotFocusNum !== undefined; });
  
  if (!hasHotData) {
    html += '<div style="text-align:center;padding:80px 20px;color:var(--text3);font-size:13px">' +
      '⚠️ 热点数据尚未接入<br><br>' +
      '含：让球数、关注热度、冷热指数、主客队特征、亚指临盘<br>' +
      '请等待后端数据接口就绪</div>';
    html += '<div class="pk2-footer"><button class="pk2-done-btn" onclick="closePK()">完成</button></div>';
    modal.innerHTML = html;
    return;
  }

  html += '<div class="pk2-section-label">热度维度指标对比</div>';
  html += '<div class="pk2-table-wrap"><table class="pk2-table">';
  
  html += '<thead><tr><th class="pk2-th-name">对阵</th>' +
    '<th class="pk2-th-num">关注热度<br>占比</th>' +
    '<th class="pk2-th-num">冷热指数<br>占比</th>' +
    '<th class="pk2-th-num">静态实力差<br>占比</th>' +
    '<th class="pk2-th-num">亚指临盘<br>占比</th>' +
    '</tr></thead><tbody>';

  var colSums = { hot: 0, heat: 0, diff: 0, odds: 0 };
  list.forEach(function (item) {
    colSums.hot  += Math.abs(parseFloat(item.hotFocusNum) || 0);
    colSums.heat += Math.abs(parseFloat(item.heatIndex) || 0);
    colSums.diff += Math.abs(parseFloat(item.staticDiff) || 0);
    colSums.odds += Math.abs(parseFloat(item.oddsLive) || 0);
  });

  list.forEach(function (item, i) {
    var hot  = parseFloat(item.hotFocusNum) || 0;
    var heat = parseFloat(item.heatIndex) || 0;
    var diff = parseFloat(item.staticDiff) || 0;
    var odds = parseFloat(item.oddsLive) || 0;
    
    html += '<tr class="pk2-row">' +
      '<td class="pk2-td-name">' +
        '<span class="pk2-row-idx">' + (i + 1) + '</span>' +
        '<span class="pk2-team-name">' + esc(item.homeName) + ' vs ' + esc(item.visitName) + '</span>' +
        '<div class="pk2-team-num">' + esc(item.num) + '</div></td>' +
      tdPctBar(hot,  colSums.hot) +
      tdPctBar(heat, colSums.heat) +
      tdPctBar(diff, colSums.diff) +
      tdPctBar(odds, colSums.odds) +
      '</tr>';
  });

  html += '<tr class="pk2-row-total"><td class="pk2-td-name"><b>列合计</b></td>' +
    tdPct(colSums.hot, colSums.hot) +
    tdPct(colSums.heat, colSums.heat) +
    tdPct(colSums.diff, colSums.diff) +
    tdPct(colSums.odds, colSums.odds) +
    '</tr>';
  html += '</tbody></table></div>';
  
  html += '<div class="pk2-footer"><button class="pk2-done-btn" onclick="closePK()">完成</button></div>';
  modal.innerHTML = html;
}
```

---

#### 改动 3：`preview/js/main.js`

找到 `startPK` 导入处（约14行），确认无需改导入语句。但需确保 `quant-rank.js` 中的 `startPK()` 已传入 `currentTab`（见改动1e）。

**无需修改 main.js** — 因为 `startPK` 已经在 onload 时挂载到了 `window` 上，HTML 中的 `onclick="startPK()"` 直接调用 `quant-rank.js` 导出的函数，该函数内部会使用闭包中的 `currentTab`。

---

#### 改动 4：`preview/index.html` CSS 补充

在量化排行榜样式区新增进球/热点列宽的 CSS 类：

```css
/* 进球排行榜列宽 */
.th-sum   { width: 48px; }
.th-big   { width: 60px; }
.th-ag    { width: 58px; }
.th-sg    { width: 58px; }
.th-hg    { width: 58px; }
.th-ba    { width: 48px; }

/* 热点排行榜列宽 */
.th-rq    { width: 48px; }
.th-hot   { width: 58px; }
.th-heat  { width: 60px; }
.th-hf    { width: 80px; }
.th-gf    { width: 80px; }
.th-sd    { width: 60px; }
.th-ol    { width: 58px; }
```

---

## 四、实施步骤

### Step 1：扩展 quant-rank.js

| 任务 | 位置 | 说明 |
| :--- | :--- | :--- |
| 1.1 | `mergeItem()` 约107行 | 新增 goal 和 hot 维度字段，含 `computeGoalTotalSum` 辅助 |
| 1.2 | `renderTable()` 约130行 | 按 `currentTab` 切换 cols 和行渲染逻辑 |
| 1.3 | 新增 `tdGoalCell()` / `tdHotCell()` | 进球和热点tab的单元格函数 |
| 1.4 | `getSortVal()` 约227行 | 扩展 goal/hot 的排序键 |
| 1.5 | `startPK()` 约55行 | 传入 `currentTab` 作为第二参数 |

### Step 2：扩展 match-pk.js

| 任务 | 位置 | 说明 |
| :--- | :--- | :--- |
| 2.1 | `openPKMulti()` 约8行 | 接受 `tab` 参数并路由到对应渲染函数 |
| 2.2 | 原 `renderPK()` → `renderPKGoal()` | 函数重命名，逻辑不变 |
| 2.3 | 新增 `renderPKPower()` | 实力维度PK表格+结果卡 |
| 2.4 | 新增 `renderPKHot()` | 热度维度PK表格（含空数据降级） |
| 2.5 | 新增辅助：`tdPctBar()` `renderPowerDuel()` `renderPowerPKCard()` | PK渲染工具函数 |

### Step 3：CSS 补充

| 任务 | 位置 | 说明 |
| :--- | :--- | :--- |
| 3.1 | `index.html` 量化表格 CSS 区约1884行后 | 新增 `.th-sum .th-big .th-ag .th-sg .th-hg .th-ba .th-rq .th-hot .th-heat .th-hf .th-gf .th-sd .th-ol` 列宽 |

### Step 4：热点数据后端接入（后续）

| 任务 | 说明 |
| :--- | :--- |
| 4.1 | 后端新建 API `/api?action=hot-data` | 整合 odds_history + gongshoudao 返回热数据 |
| 4.2 | `quant-rank.js` 层加载 hot 数据 `Promise.all` | 与 ranking-list / gongshoudao 并行请求 |
| 4.3 | `mergeItem()` 中填充 hot 真实值 | 替换 `'-'` 占位符 |

---

## 五、注意事项

1. **PK 列合计按选中行重新计算**：`renderPKPower/Goal/Hot` 中 `colSums` 仅计算传入的 `list`，符合产品文档"PK模式下仅基于勾选行计算"的需求。

2. **零点除保护**：`tdPct()` 中 `sum > 0` 判断防止 `NaN`。

3. **热点 tab 降级策略**：当 `hotFocusNum` 为 `'-'` 时，渲染"数据接入中"占位。PK 弹窗同样检测并显示友好提示。

4. **CSS 类 `.cool` 需新增**：用于冷热指数 `< 0.8` 的蓝色样式：
   ```css
   .q-cell-num.cool { color: #3498DB; }
   ```

5. **现有文件最小改动原则**：不重写整个文件，只做精确的 `replace_in_file` 编辑，避免引入额外 bug。

6. **sortAsc/sortKey 状态管理**：`sortAsc` 和 `sortKey` 是模块级变量，切换 tab 后排序状态仍然保留，但不同 tab 的列键不同，建议在 `switchQuantTab()` 中重置：`sortKey = currentTab === 'power' ? 'rank' : currentTab === 'goal' ? 'totalSum' : 'hotFocusNum'; sortAsc = true;`

---

## 六、测试要点

| 测试场景 | 验证点 |
| :--- | :--- |
| 切换到进球tab | 显示7列（对阵+合计+大球比例+攻防进球+实力进球+交锋进球+破甲和） |
| 切换到热点tab | 显示8列，数值列为"数据接入中" |
| 进球tab排序 | 点击合计/大球比例等列头正确升降序 |
| 实力tab勾选2场PK | PK弹窗显示实力维度对比表+PK结果卡 |
| 进球tab勾选2场PK | PK弹窗显示进球维度对比表+PK结果卡 |
| 热点tab勾选2场PK | PK弹窗显示"数据尚未接入"提示 |
| 跨tab勾选 | `pickedIds` 保持（切换tab不清空选中） |
| 列合计为0 | 占比显示为 `0%`，不抛异常 |
