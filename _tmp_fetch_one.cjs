const https=require('https');
function post(action,data){return new Promise((resolve,reject)=>{const body=JSON.stringify(Object.assign({action:action},data||{}));const req=https.request('https://zj.100qiu.com/api',{method:'POST',headers:{'Content-Type':'application/json','Host':'zj.100qiu.com','Content-Length':Buffer.byteLength(body)},rejectUnauthorized:false},res=>{let s='';res.on('data',c=>s+=c);res.on('end',()=>{try{resolve(JSON.parse(s));}catch(e){reject(e);}})});req.on('error',reject);req.write(body);req.end();});}
(async()=>{const d='2026-04-13';const p=await post('plan-list',{date:d,qualityMode:'all',includeReasons:1});console.log('plan-list code=',p.code,'plans=',(p.data&&p.data.plans?p.data.plans.length:0),'notice=',p.data&&p.data.notice);const m=await post('match-list',{date:d});console.log('match-list code=',m.code,'matches=',Array.isArray(m.data)?m.data.length:0);const ids=(Array.isArray(m.data)?m.data:[]).map(x=>String(x.id||x.matchId)).slice(0,7);const o=await post('batch-match-odds',{date:d,matchIds:ids});console.log('odds keys=',Object.keys(o.data||{}).length,'sample=',JSON.stringify((o.data||{})[ids[0]]||{}).slice(0,220));})();