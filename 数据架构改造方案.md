# JC-ZJFA 数据架构全面改造方案

> **版本**: V3.0  
> **编制日期**: 2026-06-06  
> **方案覆盖**: 数据采集 → 清洗 → 存储 → API → 前端展示 → 部署 → 监控 → 安全  
> **总计**: 13 Phase，预估工期 35-40 天  
> **核心原则**: ① 数据安全优先，不中断线上服务；② 本地充分测试再上生产；③ 不过度设计，够用即止

---

## 目录

1. [现状诊断](#1-现状诊断)
2. [改造前置认知与安全基线](#0-改造前置认知与安全基线)
3. [Phase 1: 紧急修复 — API 鲁棒性](#phase-1-紧急修复--api-鲁棒性)
4. [Phase 2: 存储统一 — 双轨合并](#phase-2-存储统一--双轨合并)
5. [Phase 3: 激活孤儿数据 — 竞彩管线接入](#phase-3-激活孤儿数据--竞彩管线接入)
6. [Phase 4: 数据质量自动化](#phase-4-数据质量自动化)
7. [Phase 5: 前端展示增强](#phase-5-前端展示增强)
8. [Phase 6: 爬虫统一编排管理](#phase-6-爬虫统一编排管理)
9. [Phase 7: Skill 增强开发/测试/部署管道](#phase-7-skill-增强开发测试部署管道)
10. [Phase 8: 文件防损坏](#phase-8-文件防损坏)
11. [Phase 9: 备份统一化](#phase-9-备份统一化)
12. [Phase 10: 错误码标准化 + API 路由拆分](#phase-10-错误码标准化--api-路由拆分)
13. [Phase 11: 前端部署优化 + WebSocket 实时推送](#phase-11-前端部署优化--websocket-实时推送)
14. [Phase 12: 监控告警完善 + Metrics 端点](#phase-12-监控告警完善--metrics-端点)
15. [Phase 13: 安全加固 + 旧代码清理](#phase-13-安全加固--旧代码清理)
16. [部署安全协议](#14-部署安全协议)
17. [实施路线图](#15-实施路线图)
18. [关键指标对比](#16-关键指标对比)
19. [预测模型优化路线（Phase 14-18）⭐](#19-预测模型与算法优化路线phase-14-18新增)
20. [改造常见坑位预警](#17-改造常见坑位预警)
21. [改造完成确认单](#18-改造完成确认单)

---

## 1. 现状诊断

### 1.1 数据存储全景

```
数据库层 (SQLite midou_data.db)
├── matches              ✅ 比赛基础信息 (midou310 API)
├── recommends           ✅ 推荐数据
├── crawl_logs           ✅ 爬取日志
├── ai_predictions       ✅ AI 预测缓存
├── odds_history_v2      ⚠️ 赔率历史 (有数据，但 batch-match-odds 不读)
├── h2h_history          ⚠️ 交锋历史 (极少写入)
├── league_standings     ⚠️ 积分榜 (极少写入)
├── data_lineage         ❌ 空表 (未使用)
├── feature_store        ❌ 空表 (未使用)
├── unified_predictions  ❌ 空表 (未使用)
├── jczq_basic_cache     ⚠️ 竞彩缓存 (部分使用)
└── prediction_outcomes  ⚠️ 预测回填 (部分使用)

JSON 文件层 (350+ 冗余文件)
├── odds_history/        76 天  ✅ 主动使用 (batch-match-odds 唯一数据源)
├── sporttery_odds/     180+ 场 ❌ 孤儿数据 (无消费者)
├── sporttery_preview/  170+ 场 ❌ 孤儿数据 (无消费者) — 含 7 大分析模块
├── sporttery_schedule/   1 份  ❌ 孤儿数据 (无消费者)
├── ttyingqiu_data/      30+ 个 ⚠️ 临时缓存 (raw + allplays + CSV 混合)
├── shuju_data/          N 个   ⚠️ 500.fenxi 分析数据
└── stats_bank.json      1 个   ✅ 功守道统计缓存
```

### 1.2 三条数据管线对比

| 管线 | 数据源 | 存储 | API 消费者 | 问题 |
|------|--------|------|------------|------|
| **赔率管线** | 500.com API | odds_history/*.json → odds_history_v2 SQLite | batch-match-odds (仅 JSON) ❌ | JSON 单点故障 |
| **赛事管线** | midou310 API | data.json | match-list / gongshoudao ✅ | 正常 |
| **竞彩管线** | lottery.gov.cn | sporttery_* 目录 | 无 ❌ | 350+ 文件完全断连 |

### 1.3 爬虫程序清单（20+ 个）

**实时抓取 (7 个)**：
| 脚本 | 数据源 | 抓取内容 |
|------|--------|----------|
| `scrape_sporttery.py` | lottery.gov.cn | 全量赔率 (SPF/RQSPF/BF/JQS/BQC) + 赛事前瞻 |
| `scrape_sporttery_odds.py` | sporttery.cn | BQC/BF/JQS 赔率 (异步 Playwright) |
| `scrape_sporttery_bqc_first.js` | sporttery.cn | BQC 最早赔率记录 |
| `fetch_500_fenxi.py` | odds.500.com | 近期战绩 + 攻防数据 |
| `fetch_500_fenxi_selenium.py` | odds.500.com | Selenium 备用方案 |
| `fetch_h2h.js` | 500.com | 两队历史交锋 |
| `fetch_league_standings.py` | liansai.500.com | 联赛积分榜 |

**回填脚本 (5)**：`batch_fetch_odds.js`、`catch_up.js`、`backfill_jczq_basic.js`、`backfill_odds_500.js`、`backfill_sporttery_hh.js`  
**合并迁移 (5)**：`merge_500_allplays.py`、`merge_db_odds.js`、`merge_odds_to_db.py`、`remap_to_midou.js`、`migrate_odds_to_db.js`

### 1.4 "没有赔率可用"的根因

#### 致命缺陷：batch-match-odds 缺少 SQLite 回退

```
match-odds (单场查询):                  batch-match-odds (批量查询):
    1) allplays.json          ← 快        1) odds_history JSON  ← 只读 JSON 文件
    2) odds_history_v2 SQLite ← 稳        2) dateKey 降级查找    ← 跨天回退
    3) odds_history JSON      ← 兜底        ── 缺陷: 无 SQLite 回退 ──

结果: 三层保护，至少有一层能命中        结果: 只依赖 JSON 文件，断裂就全军覆没
```

**故障时间线**：
```
08:00  midou310 抓取成功 → data.json 写入当天比赛
12:00  data_sync 500.com 反爬封禁 → odds_history/当天.json 不存在
14:00  前端调用 batch-match-odds → getOddsHistory() → 文件不存在 → 返回 {}
      此时 midou_data.db 的 odds_history_v2 有数据 → 但 batch-match-odds 从未查过它
```

#### 其他断点

| 优先级 | 根因 | 触发条件 |
|--------|------|----------|
| P0 | `batch-match-odds` 缺少 SQLite 回退 | 500.com 抓取失败 + JSON 文件不存在 |
| P0 | 500.com / midou310 两条管线独立 | 任意一方失败 |
| P1 | `data_sync.js` 文件存在即跳过（不校验内容） | 过期赔率文件未被覆盖 |
| P2 | `num` 编号跨天碰撞 | 同日编号重叠 + 跨天降级 |
| P3 | 7 天降级耗尽 | 连续 7 天 500.com 全部失败 |

### 1.5 文件损坏与备份问题

**文件损坏根因**：
- SQLite `journal_mode = WAL` + `synchronous = NORMAL`：进程崩溃时数据不一致
- `_saveToFile()` 直接 `writeFileSync` 覆盖，非原子写入
- 无启动时 `PRAGMA integrity_check` 校验
- 7+ 个脚本使用直接 `writeFileSync` 而非原子写入

**备份冗余**：
- 4 套独立备份系统 (deploy.py / backup_db.py / _rebuild_db.py / _rebuild3.py)
- 5/10 个文件被双重备份 (重复率 71%)
- 无自动清理机制，月磁盘增长 ~5.4GB

### 1.6 项目短板全景

| 领域 | 问题 |
|------|------|
| **架构** | 2 套并行服务器 (index.js 6000+ 行 vs simple.js)、WebSocket 未实现、API 单端点无版本管理 |
| **可靠性** | 错误返回码不分"无数据"vs"错误"、midou310 Token 无主动监控、PM2 restart 非零停机 |
| **数据完整性** | 比赛状态回填无本地验证、用户方案存储纯 JSON 无事务保护 |
| **监控告警** | 告警覆盖不全 (缺连续性检测和磁盘监控)、无 Prometheus metrics |
| **安全** | ecosystem.config.json 明文密码、API 无 Key 校验、user_plans 无权限隔离 |
| **开发体验** | 主文件 6000+ 行难以维护、测试覆盖不均衡、前端 7 天强缓存致部署后不生效 |

---

## 0. 改造前置认知与安全基线

### 0.1 双后端架构（必须理解后再动手）

项目 `database.js` 存在两套 SQLite 后端，**本地 Windows 和生产 CentOS 6 走不同路径**：

| 环境 | 后端 | 机制 | 关键差异 |
|------|------|------|----------|
| **本地 Windows** | `better-sqlite3` (native addon) | WAL 模式直写磁盘，**无 `_saveToFile()`** | `execRun` 直接落盘，靠 WAL 保证一致性 |
| **生产 CentOS 6** | `sql.js` (纯 JS WASM) | 内存操作 + `_saveToFile()` 手动写盘 | `execRun` 先改内存，再 export 全量写文件 |

**这意味着**：
- Phase 8 的原子写入修复**仅适用于 sql.js 路径**（better-sqlite3 没有 `_saveToFile`）
- better-sqlite3 的风险在**WAL 文件膨胀**和**崩溃时 WAL 未 checkpoint**，需要不同策略
- **所有代码改动必须在两种后端上都验证通过**才能上生产

### 0.2 PM2 进程架构（实际配置）

```
ecosystem.config.json:
├── jc-zjfa (fork, instances:1)  ← Express API 主进程
└── jc-sync (fork, instances:1)  ← data_sync.js 守护进程
```

**两个进程都读写 `midou_data.db`**，虽然非 cluster 模式，但仍存在**跨进程并发写竞争**：
- jc-sync 写入 odds/matches 数据时，jc-zjfa 可能同时在读或写
- SQLite 的 `busy_timeout = 3000` 能处理锁等待，但长时间持锁会导致 API 超时

### 0.3 改造铁律（不可逾越）

| 铁律 | 内容 |
|------|------|
| **只增不改** | 新增表/字段/函数，**不删除、不重命名**现有表和字段 |
| **向后兼容** | 所有新增 API handler 保留旧行为，新行为通过 `action` 或 version 参数触发 |
| **先本地后生产** | 每个 Phase 在本地 Windows 跑通 → CentOS 沙箱验证 → 生产灰度 |
| **随时可回滚** | 每个改动保留 git commit，部署前 `cp` 全量备份 |
| **不中断服务** | 部署用 `pm2 reload` 而非 `pm2 restart`，改造期间不停 API |
| **不过度设计** | 已有但未用的空表（data_lineage/feature_store/unified_predictions）只做记录，不强制激活 |

### 0.4 本地测试环境要求

```
本地 Windows 测试:
├── better-sqlite3 路径: npm install → 自动选中
├── sql.js 路径模拟: 临时注释 better-sqlite3 的 require.resolve → 强制走 sql.js
└── 两套后端都要跑通: node -e "require('./server/database')"

生产 CentOS 沙箱:
├── 复制 midou_data.db 到 /tmp/test_midou_data.db
├── 启动测试实例: PORT=3001 node index.js
├── 验证 API: curl localhost:3001/api -d '{"action":"health"}'
└── 确认无异常后: 正式部署
```

---

## Phase 1: 紧急修复 — API 鲁棒性

**工期**: 1-2 天 | **优先级**: P0

### 修复方案：batch-match-odds 注入 SQLite 回退

**改动位置**：仅修改 `server/index.js` 中 `case 'batch-match-odds':` 分支（约第 5496 行），在 `getOddsHistory(dateStr)` 返回空时追加 SQLite 回退。**不改动 `cache.js`**（避免影响 match-odds 等其他调用方）。

同时修改 `server/core/cache.js` 的 `getOddsHistory()` 函数（约第 90 行），在该函数 JSON 读取失败时增加 SQLite 回退逻辑，这样所有调用 `getOddsHistory` 的地方（包括 `batch-match-odds` 和 `match-odds` 的第三层回退）都能受益。

```js
case 'batch-match-odds': {
    var dateStr = data.date || latestDataDate();
    var oddsMap = getOddsHistory(dateStr) || {};

    // ★ P0 修复：JSON 为空时回退到 SQLite odds_history_v2
    if (Object.keys(oddsMap).length === 0) {
      try {
        var adp = database.getAdapter();
        var v2Rows = adp ? adp.execAll(
          'SELECT * FROM odds_history_v2 WHERE date = ?', dateStr
        ) : [];
        if (v2Rows.length > 0) {
          oddsMap = {};
          v2Rows.forEach(function(row) {
            var n = row.match_num;
            if (!oddsMap[n]) {
              oddsMap[n] = {
                num: n, homeName: row.home_name,
                visitName: row.visit_name, handicap: row.handicap
              };
            }
            var playData;
            try { playData = JSON.parse(row.odds_json); } catch(e) { return; }
            var entry = oddsMap[n];
            if (row.play_type === 'spf' || row.play_type === 'rqspf') {
              entry[row.play_type] = playData;
            } else if (row.play_type === 'halfFull') {
              entry.halfFull = playData;
            } else if (row.play_type === 'totalGoals') {
              entry.totalGoals = playData;
            } else if (row.play_type === 'scores') {
              entry.scores = playData;
            }
          });
        }
      } catch (e) { /* 回退失败不影响 */ }
    }
    // ... 后续逻辑不变
```

**预期效果**：batch-match-odds 覆盖率从 60% → 95%

---

## Phase 2: 存储统一 — 双轨合并

**工期**: 3-5 天 | **优先级**: P1 | **风险**: 🔴 高

### 核心原则：三步走，每步观察 1 小时

```
Day 1: 建表 (只增不改)
  → database.js NEW_TABLES_DDL 中新增 odds_wide 表
  → 部署 → 观察 1 小时 → PM2 无异常

Day 2: 改读取 (渐进切换)
  → cache.js 新增 getOddsUnified()，优先 odds_wide
  → batch-match-odds / match-odds 改为调用 getOddsUnified
  → 部署 → 观察 2 小时 → 前端赔率正常

Day 3: 改写入 (数据源头)
  → data_sync.js sync500Odds 改为双写 (SQLite + JSON)
  → 部署 → 观察 4 小时 → 无报错

关键: 永远保留 JSON 写入作为镜像备份，不受影响
```

### 改造目标

```
改造前:                            改造后:
┌──────────────┐                  ┌──────────────────┐
│ JSON 写入      │                 │ SQLite 写入        │  ← 唯一写入路径
│ (data_sync)    │                 │ (统一 sync 模块)    │
│       ↓       │                 │       ↓           │
│ JSON 文件      │ ← API 主读取    │ midou_data.db     │  ← API 主读取
│       ↕       │                 │       ↓           │
│ SQLite 迁移    │ ← 手动执行      │ JSON 缓存          │  ← 自动同步输出
│ (migrate_*)    │                 │ (只读/备份/跨机)    │
└──────────────┘                  └──────────────────┘
```

### 2.1 统一赔率宽表

```sql
-- 统一赔率宽表（一次查询拿到所有玩法）
CREATE TABLE IF NOT EXISTS odds_wide (
    match_num TEXT NOT NULL,
    date TEXT NOT NULL,
    match_id TEXT,
    spf_json TEXT,       -- {"home":4.85,"draw":3.9,"away":1.5}
    rqspf_json TEXT,
    bf_json TEXT,        -- scores 比分
    jqs_json TEXT,       -- totalGoals 总进球
    bqc_json TEXT,       -- halfFull 半全场
    handicap REAL,
    home_name TEXT,
    visit_name TEXT,
    league_name TEXT,
    is_single_game INTEGER DEFAULT 0,
    updated_at TEXT DEFAULT (datetime('now','localtime')),
    PRIMARY KEY (match_num, date)
);
CREATE INDEX IF NOT EXISTS idx_odds_wide_date ON odds_wide(date);
```

### 2.2 data_sync.js 双写改造

```js
async function sync500Odds(dateStr) {
    const odds = await fetch500Odds(dateStr);
    if (Object.keys(odds).length === 0) return;

    // 1) 写入 SQLite (主存储)
    await writeOddsToSQLite(dateStr, odds);

    // 2) 写入 JSON (镜像缓存)
    fs.writeFileSync(filePath, JSON.stringify({ date: dateStr, odds }));

    // 3) 更新数据新鲜度标记
    await updateDataFreshness(dateStr, 'odds');
}
```

### 2.3 API 读取路径改造

在 `cache.js` 新增统一读取函数：

```js
function getOddsUnified(dateStr) {
  // 1) 尝试 SQLite odds_wide
  // 2) 降级 odds_history_v2
  // 3) 降级 JSON 文件
  // 4) 7天前向降级
  return getOddsHistory(dateStr);
}
```

### 2.4 内容感知刷新（替代文件大小校验）

```js
async function needsRefresh(dateStr, filePath) {
  if (!fs.existsSync(filePath)) return true;
  var raw = JSON.parse(fs.readFileSync(filePath, 'utf8'));
  var odds = raw.odds || {};
  // 1) 比赛数校验
  var dbCount = /* 从 matches 表查当天应有比赛数 */;
  if (Object.keys(odds).length < dbCount * 0.8) return true;
  // 2) 时效校验 (1小时内刷新过)
  var age = Date.now() - (raw.updated ? new Date(raw.updated).getTime() : 0);
  if (age > 3600000) return true;
  return false;
}
```

---

## Phase 3: 激活孤儿数据 — 竞彩管线接入

**工期**: 3-5 天 | **优先级**: P2

### 问题回顾

- `scrape_sporttery.py` 产出 350+ 文件，**完全未被使用**
- 数据比 500.com 赔率多了：**时间序列变更历史（含涨跌标记）** + **7 大前瞻分析模块**

### 3.1 竞彩赛事前瞻表

```sql
CREATE TABLE IF NOT EXISTS sporttery_preview (
    match_id TEXT NOT NULL,
    date TEXT NOT NULL,
    match_num TEXT,              -- 映射后的竞彩编号 周五201
    home_team TEXT,
    away_team TEXT,
    league TEXT,
    feature_analysis TEXT,       -- 特征分析 JSON
    h2h_history TEXT,            -- 历史交锋 JSON
    standings TEXT,              -- 积分榜 JSON
    recent_form TEXT,            -- 比赛近况 JSON
    future_matches TEXT,         -- 未来赛事 JSON
    scorers TEXT,                -- 射手信息 JSON
    injuries TEXT,               -- 伤停一览 JSON
    created_at TEXT DEFAULT (datetime('now','localtime')),
    PRIMARY KEY (match_id, date)
);
```

### 3.2 赔率变更快照表（时间序列）

```sql
CREATE TABLE IF NOT EXISTS sporttery_odds_snapshot (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    match_id TEXT NOT NULL,
    date TEXT NOT NULL,
    play_type TEXT NOT NULL,     -- spf/rqspf/bf/jqs/bqc
    snapshot_time TEXT NOT NULL, -- 抓取时间
    odds_json TEXT NOT NULL,     -- 该时刻的赔率快照
    trend TEXT,                  -- ↑ ↓ 涨跌标记
    created_at TEXT DEFAULT (datetime('now','localtime'))
);
CREATE INDEX IF NOT EXISTS idx_odds_snap_match ON sporttery_odds_snapshot(match_id, date, play_type);
```

### 3.3 映射桥接脚本

创建 `scripts/bridge_sporttery_to_odds.js`：将 sporttery matchId (1022xxx) → 竞彩编号 (周五201)，写入统一表。

### 3.4 数据管线对比（接入前后）

| 数据 | 接入前状态 | 接入后通路 |
|------|-----------|-----------|
| `sporttery_odds/` 时间序列赔率 | ❌ 断连 | → `sporttery_odds_snapshot` → `odds-trend` API → 前端走势图 |
| `sporttery_preview/` 7 大模块 | ❌ 断连 | → `sporttery_preview` → `match-preview` API → 功守道前瞻 Tab |
| `sporttery_odds/` 开奖结果 | ❌ 未使用 | → 本地赛果验证 → 自动化对账 |

---

## Phase 4: 数据质量自动化

**工期**: 2-3 天 | **优先级**: P1

### 4.1 新鲜度检测表

```sql
CREATE TABLE IF NOT EXISTS data_freshness (
    data_type TEXT NOT NULL,       -- odds/matches/recommends/preview
    date TEXT NOT NULL,
    last_updated TEXT NOT NULL,
    row_count INTEGER,
    status TEXT DEFAULT 'ok',      -- ok/stale/missing
    checked_at TEXT DEFAULT (datetime('now','localtime')),
    PRIMARY KEY (data_type, date)
);
```

### 4.2 定时完整性校验

在 `scheduler_v2.js` 中新增每 30 分钟巡检：

```js
async function freshnessCheck() {
  const checks = [
    { type: 'odds',     query: 'SELECT COUNT(*) as cnt FROM odds_wide WHERE date = ?' },
    { type: 'matches',  query: 'SELECT COUNT(*) as cnt FROM matches WHERE date = ?' },
    { type: 'preview',  query: 'SELECT COUNT(*) as cnt FROM sporttery_preview WHERE date = ?' },
  ];
  // 发现缺失 → 自动触发回填
}
```

### 4.3 触发告警的条件

| 告警条件 | 级别 | 动作 |
|----------|------|------|
| odds_history 连续 2 天空 | P0 | 自动回填 + 邮件告警 |
| midou310 连续 3 次登录失败 | P0 | 邮件告警 + 切换备用账户 |
| 数据库损坏检测 | P0 | 自动备份 + 恢复 + 邮件告警 |
| 抓取成功率 < 90% (连续 3 次) | P1 | 邮件告警 |
| 磁盘使用 > 80% | P1 | 邮件告警 |

---

## Phase 5: 前端展示增强

**工期**: 2-3 天 | **优先级**: P2

### 5.1 新增 API

| API | 数据源 | 前端页面 |
|-----|--------|---------|
| `odds-trend` | `sporttery_odds_snapshot` | 方案设计页、PK 弹窗（赔率走势箭头） |
| `match-preview` | `sporttery_preview` | 功守道弹窗（新增"赛事前瞻"Tab） |
| `scraper-dashboard` | `crawl_logs` + 注册表 | 数据健康页（爬虫状态看板） |

### 5.2 SWR 缓存策略

```js
// main-fusion.js 中增加 Stale-While-Revalidate 策略
var _swrCache = {};
function swrFetch(key, fetcher, ttl) {
  if (_swrCache[key] && Date.now() - _swrCache[key].time < ttl) {
    return Promise.resolve(_swrCache[key].data);
  }
  return fetcher().then(function(data) {
    _swrCache[key] = { data: data, time: Date.now() };
    return data;
  });
}
```

---

## Phase 6: 爬虫统一编排管理

**工期**: 4-5 天 | **优先级**: P1

### 6.1 爬虫注册配置文件

创建 `server/scraper_registry.json`，所有爬虫在此注册：

```json
{
  "version": "2.0",
  "scrapers": {
    "sporttery_odds": {
      "name": "竞彩赔率抓取",
      "script": "python scripts/scrape_sporttery.py",
      "type": "python",
      "source": "lottery.gov.cn",
      "category": "odds",
      "schedule": { "cron": "0 12 * * *", "label": "每天12:00" },
      "target_table": "odds_history_v2",
      "target_dir": "server/sporttery_odds/",
      "timeout_sec": 600,
      "retry": { "max": 3, "backoff": "exponential", "initial_min": 5 },
      "quality_check": { "min_records": 5, "required_fields": ["tables"] }
    },
    "sporttery_preview": {
      "name": "竞彩赛事前瞻",
      "script": "python scripts/scrape_sporttery.py",
      "type": "python",
      "source": "sporttery.cn (showType=2)",
      "category": "analysis",
      "schedule": { "cron": "30 12 * * *", "label": "每天12:30" },
      "target_table": "sporttery_preview",
      "timeout_sec": 600,
      "quality_check": { "min_records": 5, "required_fields": ["h2h", "standings"] }
    },
    "500_fenxi": {
      "name": "500.com 攻防分析",
      "script": "python scripts/fetch_500_fenxi.py",
      "type": "python",
      "source": "odds.500.com/fenxi",
      "category": "analysis",
      "schedule": { "cron": "0 13 * * *", "label": "每天13:00" },
      "timeout_sec": 300,
      "fallback": "500_fenxi_selenium"
    },
    "h2h_history": {
      "name": "历史交锋抓取",
      "script": "node scripts/fetch_h2h.js",
      "type": "node",
      "source": "500.com",
      "category": "h2h",
      "schedule": { "cron": "0 14 * * *", "label": "每天14:00" },
      "target_table": "h2h_history",
      "quality_check": { "min_records": 3 }
    },
    "league_standings": {
      "name": "联赛积分榜",
      "script": "python scripts/fetch_league_standings.py",
      "type": "python",
      "source": "liansai.500.com",
      "category": "standings",
      "schedule": { "cron": "0 14 * * *", "label": "每天14:00" },
      "target_table": "league_standings"
    }
  },
  "pipelines": {
    "daily_full_sync": {
      "name": "每日全量同步流水线",
      "triggers_on": "noon_batch",
      "steps": [
        { "scraper": "sync_match_list", "depends_on": [] },
        { "scraper": "sync_500odds", "depends_on": ["sync_match_list"], "delay_sec": 30 },
        { "scraper": "sporttery_odds", "depends_on": ["sync_match_list"], "delay_sec": 60 },
        { "scraper": "sporttery_preview", "depends_on": ["sporttery_odds"] },
        { "scraper": "500_fenxi", "depends_on": ["sync_500odds"], "delay_sec": 120 }
      ]
    }
  }
}
```

### 6.2 插件化架构：注册即用

新增爬虫只需 **一条 JSON 配置 + 一个脚本**，自动获得：

| 能力 | 触发方式 |
|------|---------|
| Cron 调度 | `schedule.cron` 自动注册定时器 |
| 依赖编排 | `pipelines.depends_on` 自动 DAG 执行 |
| 失败重试 | `retry` 配置 → 指数退避自动入队 |
| 质量校检 | `quality_check` → 抓取后自动校验 |
| 新鲜度监控 | 自动纳入 `data_freshness` 巡检 |
| 健康看板 | 自动出现在 `scraper-dashboard` API |
| JSON→SQLite 迁移 | 配了 `target_table` 则自动触发 |
| 分布式锁 | 长任务自动抢锁防并发 |

### 6.3 统一调度器核心

```js
// server/unified_scraper.js — 插件化调度引擎
async function executeScraper(name, params = {}) {
  const config = registry.scrapers[name];  // 从注册表获取
  // 1) spawn 子进程
  // 2) 质量校验
  // 3) 失败自动重试
  // 4) 成功自动触发迁移
  // 5) 记录运行日志
}

async function start() {
  // 1) 验证注册表 (脚本存在性 + cron 合法性 + DDL 声明)
  // 2) 为所有有 schedule 的爬虫注册 CronJob
  // 3) 为 pipelines 注册 DAG 依赖执行
}
```

### 6.4 新增爬虫完整流程

```
Step 1: 创建抓取脚本  scripts/my_new_crawler.js
Step 2: 注册一条配置  scraper_registry.json
Step 3: 验证注册      node server/tests/registry_validate.js
Step 4: 部署          python deploy.py + pm2 restart scraper
Step 5: 检查看板      curl scraper-dashboard API
```

---

## Phase 7: Skill 增强开发/测试/部署管道

**工期**: 2-3 天 | **优先级**: P2

### 7.1 data-pipeline Skill 增强

新增**爬虫注册规范**：

```markdown
## 新增爬虫 Checklist
1. [ ] 在 scraper_registry.json 中注册
2. [ ] 目标表在 database.js NEW_TABLES_DDL 中声明
3. [ ] 迁移脚本准备就绪
4. [ ] 质量校检规则定义
5. [ ] 反爬策略测试通过（连续 3 天成功率 ≥ 90%）
6. [ ] PM2 调度注册（如果是 Node.js 守护进程）
```

### 7.2 jczjfa-test-orchestrator Skill 增加爬虫测试

| 阶段 | 已有测试 | 新增爬虫测试 |
|------|---------|-------------|
| Pre-commit | `npm run lint && test:p0` | 爬虫注册表 schema 验证 |
| 本地验证 | `npm test` (P0/P1/P2) | `node scripts/check-deploy-deps.js --scrapers` |
| 部署前 | `npm run preflight` (7 关) | `python _verify_scrapers.py` |

### 7.3 deploy-ops Skill 增加爬虫部署清单

```markdown
## 爬虫部署 Checklist
- [ ] deploy.py 默认包含 scripts/*.py
- [ ] 服务器 Python 依赖安装: pip install -r requirements.txt
- [ ] Playwright 浏览器: playwright install chromium
- [ ] PM2 注册: pm2 start server/unified_scraper.js --name scraper
- [ ] 验证: python _verify_scrapers.py
```

### 7.4 新增 Skill 触发词映射

| Skill | 新增触发词 |
|-------|-----------|
| data-pipeline | `scraper`、`爬虫注册`、`抓取调度`、`质量门禁` |
| jczjfa-test-orchestrator | `爬虫测试`、`scraper test`、`抓取验证` |
| deploy-ops | `爬虫部署`、`scraper deploy`、`Python依赖` |

---

## Phase 8: 文件防损坏

**工期**: 2 天 | **优先级**: P0

### ⚠️ 关键：两套后端的防损坏策略不同

| 后端 | 风险点 | 策略 |
|------|--------|------|
| **sql.js** (生产) | `_saveToFile()` 直接覆盖 → 半写损坏 | 原子写入（.tmp → rename） |
| **better-sqlite3** (本地) | WAL 文件膨胀 + 崩溃时 WAL 未回写 | WAL checkpoint + `synchronous=FULL` |

### 8.1 sql.js 原子写入（生产环境）

当前 `database.js` 第 222-231 行 `_saveToFile()`：

```js
// ★ 改造前 (第 222-231 行):
function _saveToFile() {
  if (_inTransaction) return;
  try {
    const data = dbInstance.export();
    const buffer = Buffer.from(data);
    fs.writeFileSync(DB_FILE, buffer);  // ← 直接覆盖！崩溃时半写
  } catch (e) { ... }
}

// ★ 改造后:
function _saveToFile() {
  if (_inTransaction) return;
  try {
    const data = dbInstance.export();
    const buffer = Buffer.from(data);
    const tmpFile = DB_FILE + '.tmp';
    fs.writeFileSync(tmpFile, buffer);
    // 写入后校验完整性
    const written = fs.readFileSync(tmpFile);
    if (written.length !== buffer.length) throw new Error('写入字节数不匹配(' + written.length + '≠' + buffer.length + ')');
    // 原子替换
    fs.renameSync(tmpFile, DB_FILE);
  } catch (e) {
    console.error('[db] 保存数据库失败: ' + e.message);
    // 保留旧 DB 文件不退化为空 → 下次启动时仍有完整数据
  }
}
```

### 8.2 better-sqlite3 WAL 防护（本地+未来生产升级）

better-sqlite3 没有 `_saveToFile()`，风险是 WAL 文件。在 `initDatabase()` 中调整 pragma：

```js
// database.js initDatabase() better-sqlite3 分支 (约第 333 行):
db.pragma('journal_mode = WAL');
db.pragma('synchronous = FULL');    // ★ NORMAL → FULL (每次提交 fsync)
db.pragma('wal_autocheckpoint = 1000');  // ★ 新增: WAL 超 1000 页自动 checkpoint
db.pragma('cache_size = -8000');
db.pragma('busy_timeout = 3000');
```

### 8.3 启动时自动完整性校验（两套后端通用）

```js
// database.js 两个 initDatabase() 分支都新增 (better-sqlite3 约 399 行后, sql.js 约 831 行后):
function _checkIntegrity() {
  try {
    const result = db.prepare('PRAGMA integrity_check').get();
    if (result['integrity_check'] !== 'ok') {
      console.error('[db] ⚠️ 数据库完整性校验失败: ' + result['integrity_check']);
      // 不自动恢复——由运维决定策略
      return false;
    }
    console.log('[db] 完整性校验通过');
    return true;
  } catch (e) {
    console.error('[db] 完整性校验异常: ' + e.message);
    return false;
  }
}

// 校验失败时的处理策略：
// 1) 本地: 人工检查 → _rebuild_db.py 重建
// 2) 生产: server/core/backup.js 恢复最近备份
```

### 8.4 统一原子写入工具（JSON 文件）

```js
// server/core/file-utils.js (新建)
const fs = require('fs');

function atomicWrite(filePath, data) {
  const tmpFile = filePath + '.tmp';
  const jsonStr = typeof data === 'string' ? data : JSON.stringify(data);
  fs.writeFileSync(tmpFile, jsonStr);
  // 验证
  const written = fs.readFileSync(tmpFile, 'utf8');
  if (written.length !== jsonStr.length) throw new Error('写入不完整(' + written.length + '≠' + jsonStr.length + ')');
  fs.renameSync(tmpFile, filePath);
  return true;
}

function atomicWriteJson(filePath, obj) {
  return atomicWrite(filePath, JSON.stringify(obj));
}

module.exports = { atomicWrite, atomicWriteJson };
```

**需要改造的文件**（7 处 → 统一引用 `file-utils.js`）：
1. `batch_fetch_odds.js` → odds_history 写入
2. `catch_up.js` → 回填赔率写入
3. `backfill_odds_500.js` → 500 赔率写入
4. `backfill_sporttery_hh.js` → 竞彩回填写入
5. `data_sync.js` → data.json 写入（已有 atomicWrite，改用统一版）
6. `gongshoudao/*.js` → cache.json 写入
7. `index.js` user_plans → 用户方案写入

### 8.5 安全验证清单

```
Phase 8 本地测试:
├── [ ] sql.js 路径: 模拟写入中途 kill 进程 → 重启后 DB 完整
├── [ ] better-sqlite3 路径: 连续大量写入 → WAL 文件不无限增长
├── [ ] PRAGMA integrity_check 在正常库返回 "ok"
├── [ ] atomicWrite 在磁盘满时抛出错误（不等同于写入空文件）
└── [ ] 7 个脚本替换后功能正常（逐个干跑验证）

Phase 8 生产部署:
├── [ ] 先备份 midou_data.db (deploy.py Phase 1 自动)
├── [ ] 先部署 file-utils.js
├── [ ] 逐个替换 JSON 写入脚本（每次替换一个 + 验证）
├── [ ] 最后替换 database.js (影响最大)
└── [ ] 用 pm2 reload 而非 restart
```

---

## Phase 9: 备份统一化

**工期**: 2 天 | **优先级**: P1

### 9.1 合并为单一备份系统

```js
// server/core/backup.js — 统一备份中心

const BACKUP_DIR = '/root/backups';
const KEEP_DAILY = 7;   // 保留最近 7 天
const KEEP_WEEKLY = 4;  // 保留最近 4 周

function backup(label = 'auto') {
  // 1) SQLite 备份（.backup API 保证一致性快照）
  // 2) JSON 文件复制
  // 3) 元信息记录
  // 4) 自动清理过期备份
}

function cleanup() {
  // 滚动清理: Daily×7 + Weekly×4
  // 额外清理 /tmp/deploy_backup_* 旧格式
  // 额外清理 .corrupted_* 残留文件
}
```

### 9.2 备份触发点

| 触发器 | 动作 |
|--------|------|
| PM2 每天 03:00 | `backup('daily')` |
| deploy.py 部署前 | 远程执行 `backup('pre_deploy')` |
| API 手动触发 | `POST /api { action: 'backup-db' }` |
| 异常检测到 DB 损坏 | `backup('pre_recovery')` → 恢复 |

### 9.3 磁盘优化

| 指标 | 优化前 | 优化后 |
|------|--------|--------|
| 备份系统数 | 4 套独立 | 1 套统一 |
| 重复备份文件 | 5/10 (71%) | 0 |
| 月磁盘增长 | ~5.4GB | ~200MB |

---

## Phase 10: 错误码标准化 + API 路由拆分

**工期**: 2.5 天 | **优先级**: P0

### 10.1 错误码体系

```js
// 当前:
return res.json({ code: 0, msg: err.message });  // 所有错误统一

// 改造后:
const ERROR_CODES = {
  OK:               0,   // 正常
  NO_DATA:          1,   // 无数据（正常业务）
  INVALID_PARAM:   -1,   // 参数错误
  SERVER_ERROR:    -2,   // 服务器内部错误
  THIRD_PARTY_ERR: -3,   // 第三方 API 失败
  DB_CORRUPT:      -4,   // 数据库损坏
  UNAUTHORIZED:    -5,   // 未授权
};
```

### 10.2 前端对应处理

```js
// 替代当前的统一错误提示
switch (resp.code) {
  case -4: showError('数据库异常，已自动恢复中...'); break;
  case -3: showError('外部数据源暂时不可用，请稍后重试'); break;
  case -2: showError('服务器繁忙，请重试'); addRetryButton(); break;
  case -1: showError('请求参数错误'); break;
  case  1: showEmpty('暂无数据'); break;
  case  0: render(resp.data); break;
}
```

### 10.3 API 路由拆分

将 `server/index.js` (6000+ 行) 按功能域拆分：

```
server/
├── routes/
│   ├── odds.js         ← match-odds / batch-match-odds / odds-trend
│   ├── plans.js        ← 方案 CRUD
│   ├── predictions.js  ← AI 预测相关
│   ├── gongshoudao.js  ← 功守道分析
│   ├── health.js       ← 健康检查 / metrics
│   └── admin.js        ← 备份 / 爬虫看板 / 管理
├── index.js            ← 路由挂载 + 中间件 (精简至 ~300 行)
```

---

## Phase 11: 前端部署优化 + WebSocket 实时推送

**工期**: 3 天 | **优先级**: P2

### 11.1 缓存策略优化

```
当前: express.static maxAge: '7d' → 部署后不生效

改造:
├── *.html           → Cache-Control: no-cache (总是验证)
├── *.abc123.js      → Cache-Control: max-age=31536000, immutable (内容哈希)
├── *.abc123.css     → Cache-Control: max-age=31536000, immutable
└── /api/*           → Cache-Control: no-store
```

### 11.2 WebSocket 实时推送

```
当前: HTTP 轮询 (比赛密集时频繁请求)

改造:
├── 赛前 2 小时 → WebSocket 推送赔率变更、推荐更新
├── 比赛中      → WebSocket 推送实时比分
├── 赛后        → WebSocket 推送最终赛果
└── 降级策略    → WS 断线自动降级到 HTTP 轮询 (15s)
```

---

## Phase 12: 监控告警完善 + Metrics 端点

**工期**: 2 天 | **优先级**: P3

### 12.1 新增 Metrics 端点

```
GET /metrics
→ PM2 进程状态 (cpu/memory/uptime)
→ 各爬虫成功率 (24h)
→ API QPS (1m)
→ 数据库大小 (MB)
→ 数据新鲜度状态
→ 抓取成功率趋势
```

### 12.2 告警规则强化

| 原告警覆盖 | 新增告警 |
|-----------|---------|
| 爬虫失败 | odds_history 连续 2 天空 |
| 调度器 fatal | midou310 连续 3 次登录失败 |
| | 数据库损坏检测 |
| | 磁盘使用 > 80% |
| | PM2 实例 offline |

---

## Phase 13: 安全加固 + 旧代码清理

**工期**: 2 天 | **优先级**: P1-P3

### 13.1 安全加固

1. **ecosystem.config.json 移除明文密码**：改为从 `.env` 读取
2. **API Key 验证**：简单静态 Key + Referer/Origin 校验
3. **user_plans 迁移到 SQLite**：替代纯 JSON 文件存储，增加事务保护
4. **部署改为零停机**：`pm2 reload` 替代 `pm2 restart`

### 13.2 旧代码归档

```
server/archive/
├── simple.js           ← 独立副本，不再维护
├── simple.js.bak
├── simple_orig.js
├── server10.js
└── index_min.js        ← 精简版，不再维护
```

### 13.3 日志轮转

```js
// logger.js → 配置 winston-daily-rotate-file
{
  filename: 'logs/app-%DATE%.log',
  datePattern: 'YYYY-MM-DD',
  maxSize: '50m',
  maxFiles: '30d'  // 30 天自动清理
}
```

---

## 14. 部署安全协议（每次 Phase 强制执行）

### 14.1 部署前检查清单

```
部署前 → 本地全部通过:
├── [ ] 两套后端都验证: better-sqlite3 + sql.js (注释切换)
├── [ ] npm run test:p0 全部通过
├── [ ] 新增/修改的 API 干跑验证 (curl 本地 :3000)
├── [ ] 数据库完整性: PRAGMA integrity_check → ok
├── [ ] git diff 检查: 无意外改动、无调试代码残留

部署前 → 生产准备:
├── [ ] 备份: python deploy.py --backup-only (或手动 cp)
├── [ ] PM2 状态确认: pm2 list (jc-zjfa + jc-sync online)
├── [ ] 磁盘空间确认: df -h (/root 剩余 > 2GB)
├── [ ] 确认改动文件清单 (git diff --name-only master)
```

### 14.2 部署步骤（每次相同）

```
Step 1: 备份
  python deploy.py --backup-only
  → /root/backups/backup_{时间戳}_pre_deploy/

Step 2: 上传静态文件（JS/CSS/HTML）
  python deploy.py --fast --files preview/css/app.css,preview/js/...
  → 浏览器 Ctrl+Shift+R 验证

Step 3: 上传服务端文件（逐文件）
  python deploy.py --fast --files server/database.js
  → 或逐个 scp，每上传一个文件等 3 秒确认 PM2 无报错

Step 4: 热重载（零停机）
  pm2 reload jc-zjfa    ← 注意: reload 不是 restart
  pm2 reload jc-sync

Step 5: 验证
  python _verify_api.py → 4 层全部通过
  浏览器: 核心页面逐个打开检查
  pm2 logs --lines 20 → 无 Error/Cannot find module

Step 6: 观察期（至少 30 分钟）
  watch -n 60 'pm2 list && du -sh /root/server/midou_data.db'
  → 确认 DB 文件大小稳定、PM2 内存无异常增长
```

### 14.3 回滚策略

```
如果部署后出问题 (任何一层验证失败):
├── 1) pm2 stop jc-zjfa && pm2 stop jc-sync
├── 2) cp /root/backups/backup_*_pre_deploy/midou_data.db /root/server/
├── 3) git checkout {last_good_commit} -- {changed_files}
├── 4) python deploy.py --fast --files {changed_files}
├── 5) pm2 start jc-zjfa && pm2 start jc-sync
├── 6) python _verify_api.py 确认恢复
└── ⚠️ 回滚后也要保留出问题的 DB 副本用于排查
```

### 14.4 各 Phase 专项安全措施

| Phase | 风险等级 | 特殊安全措施 |
|-------|---------|-------------|
| **Phase 1** | 🟡 中 | 只改 batch-match-odds 分支 + cache.js 回退逻辑，不动其他 API |
| **Phase 2** | 🔴 高 | **先建表、后改读、最后改写**。三天分步：D1建表→D2改 API 读→D3改 data_sync 写。每步间隔 1 小时观察 |
| **Phase 3** | 🟢 低 | 纯增量（新表+新脚本+新 API），不碰现有管线 |
| **Phase 4** | 🟢 低 | 新增巡检任务，失败不影响主流程 |
| **Phase 5** | 🟢 低 | 前端增量（新 API + 新组件），旧页面不受影响 |
| **Phase 6** | 🟡 中 | 调度器新旧并行 3 天，确认稳定后切换 |
| **Phase 7** | 🟢 低 | 只改 SKILL.md 文档，不影响代码 |
| **Phase 8** | 🔴 高 | **最高风险**。sql.js 原子写入在本地用 sql.js 路径验证 10 次以上 |
| **Phase 9** | 🟡 中 | 新备份系统与旧备份并行 1 周，确认无遗漏再删旧脚本 |
| **Phase 10** | 🟡 中 | 错误码改造：先加新 code，保留旧兼容 1 周，再清理 |
| **Phase 11** | 🟡 中 | 缓存改动：先在 staging 环境验证 1 天 |
| **Phase 12** | 🟢 低 | 纯增量，metrics 端点独立 |
| **Phase 13** | 🟡 中 | 密码迁移：先确认 .env 加载正常，再改 ecosystem.config.json |

---

## 15. 实施路线图

### 第一优先级：数据安全 + 核心修复（Week 1-2）

```
Week 1: Phase 8 (文件防损坏) + Phase 1 (API 回退)
  注: Phase 8 先行！先保证数据不丢，再修复读取问题
  ├── Day 1: Phase 8A — sql.js 原子写入 (本地 sql.js 路径验证 10+ 次)
  ├── Day 2: Phase 8B — better-sqlite3 WAL 防护 + integrity_check
  ├── Day 3: Phase 8C — file-utils.js + 7 个脚本逐个替换
  ├── Day 4: Phase 1  — batch-match-odds + cache.js SQLite 回退
  └── Day 5: 综合冒烟测试 + 部署 (先部署 Phase 8，隔天部署 Phase 1)

Week 2: Phase 9 (备份统一)
  ├── Day 6-7: server/core/backup.js 开发 + 本地测试
  ├── Day 8:   生产部署，与旧备份并行 (观察 3 天无遗漏)
  └── Day 9-10: 确认无问题后归档旧备份脚本
```

### 第二优先级：存储规范化（Week 3-4）

```
Week 3: Phase 2 (存储统一) — 三步走
  ├── Day 11:  建 odds_wide 表 → 部署 → 观察 1h
  ├── Day 12:  改 API 读取 (cache.js getOddsUnified) → 部署 → 观察 2h
  ├── Day 13:  改 data_sync 双写 → 部署 → 观察 4h
  └── Day 14:  Phase 4 (数据新鲜度) + 缓冲

Week 4: Phase 10 (错误码标准化) — 渐进式
  ├── Day 15:  定义 error_codes.js 常量
  ├── Day 16:  index.js 错误返回改为新 code（保留旧兼容 1 周）
  └── Day 17:  前端错误处理升级 (switch code)
```

### 第三优先级：数据激活 + 爬虫统一（Week 5-6）

```
Week 5: Phase 3 (竞彩接入) + Phase 5 (前端新 API)
  ├── Day 18-19: 建竞彩表 + bridge 桥接
  ├── Day 20:    odds-trend / match-preview API
  └── Day 21-22: 前端走势展示 + SWR 缓存

Week 6: Phase 6 (爬虫统一编排)
  ├── Day 23-24: scraper_registry.json + unified_scraper.js
  ├── Day 25:    新旧调度器并行 3 天
  └── Day 26:    scraper-dashboard API
```

### 第四优先级：体验 + 运维 + 预测基础（Week 7-8）

```
Week 7: Phase 7 (Skill) + Phase 11 (前端部署) + Phase 12 (监控) + Phase 13 (安全)
  ├── Phase 7:  Skill 文档更新 (1 天)
  ├── Phase 11: 缓存策略 + WebSocket (2 天)
  ├── Phase 12: Metrics 端点 + 告警 (1 天)
  └── Phase 13: 安全加固 + 旧代码归档 (1 天)

Week 8: 预测模型优化（Phase 14-15，数据架构改造收尾后启动）
  ├── Day 36-38: Phase 14 特征工程管道 (feature-builder.js)
  ├── Day 39-40: Phase 15 结构化预测记录 + prediction_outcomes 激活
  └── Day 41-43: Phase 16 实验追踪框架

后续 (Phase 17-18，按需推进):
  ├── 2-3 周: Phase 17 量化预测模型 (Logistic/Poisson/校准)
  └── 1 周:   Phase 18 Walk-Forward 回测增强

缓冲 / 回归测试: Day 44-48
```

---

## 16. 关键指标对比

| 指标 | 改造前 | 改造后 | Phase |
|------|--------|--------|-------|
| batch-match-odds 可用率 | ~60% | ~99% | 1 |
| 赔率数据覆盖率 | 76 天 | 完整赛季覆盖 | 2, 3 |
| 孤儿数据文件 | 350+ 断连 | 0 (全部接入) | 3 |
| 数据存储模式 | JSON+SQLite 双轨 | SQLite 单源 | 2 |
| API 响应延迟 | 100-500ms (磁盘IO) | 10-50ms (内存SQLite) | 2 |
| 数据新鲜度检测 | 无 | 30分钟自动巡检 | 4 |
| 前端数据维度 | 赔率+推荐+功守道 | +走势曲线+前瞻分析 | 5 |
| 爬虫管理 | 20+ 脚本独立运行 | 1 套注册表 + 统一调度 | 6 |
| SQLite 原子性 | ❌ 直接覆盖 | ✅ .tmp → rename | 8 |
| 启动完整性校验 | ❌ 无 | ✅ PRAGMA integrity_check | 8 |
| JSON 原子写入覆盖 | 14% (1/7) | 100% | 8 |
| 备份系统数 | 4 套独立 | 1 套统一 | 9 |
| 重复备份文件 | 71% | 0 | 9 |
| 月磁盘增长 | ~5.4GB | ~200MB | 9 |
| 错误码区分 | ❌ 全是 code:0 | ✅ 6 级错误码 | 10 |
| 主文件行数 | 6000+ 行 | ~300 + 6 路由模块 | 10 |
| 部署缓存生效 | 部署后 7 天 | 即时生效 | 11 |
| 实时推送 | HTTP 轮询 | WebSocket + 降级 | 11 |
| 监控端点 | 无 | Prometheus /metrics | 12 |
| 明文密码 | 有 | .env 环境变量 | 13 |
| 日志轮转 | 无限制 | 按天切割 + 30天清理 | 13 |
| 总工期 | — | 35-40 天 (数据架构) + 14 天 (预测优化) | — |

### 预测优化专项指标

| 指标 | 改造前 | 改造后 | Phase |
|------|--------|--------|-------|
| 特征工程 | 手动拼字符串 | 26 维标准化向量 | 14 |
| 模型类型 | 固定公式 | 公式 + ML + LLM 三层 | 17 |
| DeepSeek 输出评估 | 无法系统评估 | prediction_outcomes 自动计分 | 15 |
| Prompt 实验追踪 | 无 | experiment_runs 表 + 版本对比 | 16 |
| 回测方式 | 单次统计 | Walk-Forward 滚动窗口 | 18 |
| 预测校准 | 无 | Platt Scaling (Brier < 0.20) | 17 |
| 特征重要性 | 不可知 | 贡献度可视化 | 14+17 |
| 模型衰减检测 | 无 | 自动告警 (最近 N 天 < 历史 × 0.8) | 18 |

---

## 19-2. 前端数据管线评估与优化（Phase 19，新增）

> 基于 `preview/js/` 全部页面 + `server/index.js` 静态服务配置的逐层审查

### 评估结果：4 个级别

| 层级 | 评级 | 关键发现 |
|------|------|---------|
| **数据提取** (api.js) | ✅ 良好 | 重试 + 超时 + DeviceId 完备 |
| **数据传输** (index.js) | ⚠️ 需优化 | 7 天强缓存导致部署不生效 |
| **数据计算** (match-detail/plans) | ⚠️ 需优化 | Dutch 计算正确但混在渲染函数中 |
| **渲染展示** (全部页面) | ⚠️ 需优化 | innerHTML 为主，无 diff，缺少加载态/错误态/空态统一处理 |

### 具体发现

#### 1. API 层 — 错误码匹配需更新

```js
// api.js 第 17-18 行 — 当前
if (d.code === 1) return d.data;    // ← 仅识别 code:1 为成功
if (d.pending) return d;            // ← 特殊 pending 状态
throw new Error(d.msg || '服务器错误');

// 问题: Phase 10 改造后，正常数据返回 code:0
//       当前逻辑会导致所有正常数据都抛异常
```

**建议**（Phase 10 同步改造）：
```js
if (d.code >= 0) return d.data;            // code:0=OK, code:1=NO_DATA
if (d.code === -5) { /* redirect to login */ }
throw { code: d.code, msg: d.msg || '服务器错误' };
```

#### 2. 渲染层 — 三种状态未统一处理

```
页面数据请求后可能的三种状态:
┌── 加载中: 7 个页面中有 5 个缺少 loading spinner
├── 错误:   全部用统一"请求失败"提示（不区分网络/服务器/数据）
├── 空数据: 只有方案页有"今日暂无方案"占位
└── 正常:   直接 innerHTML 替换

问题: 用户看到错误提示但无法区分"网络断了"还是"真的没数据"
```

**建议**：在 `utils.js` 中新增 `renderStates(el, state, data)` 统一渲染工具。

#### 3. 计算层 — 数据转换嵌入渲染函数

```
match-detail.js:
  _dutchOdds() / _parseJqs() / _parseBqc()  ← 纯计算函数 ✅
  但在 renderXXX() 中直接调用 → 错误处理无法统一

建议: 提取为独立的 data-transform.js 模块
```

#### 4. 缓存层 — 缺少 SWR（Stale-While-Revalidate）

```
utils.js 缓存策略:
  match-list:  2 分钟 TTL
  plan-list:   5 分钟 TTL
  default:     2 分钟

问题: 缓存过期后 → 必须等待网络请求 → 白屏/loading
建议: SWR 策略 → 立即返回过期缓存 → 后台刷新 → 静默更新
```

#### 5. 传输层 — 7 天强缓存

```
server/index.js 第 405-416 行:
  JS/CSS: Cache-Control: public, max-age=604800, immutable
  问题: 部署后浏览器仍用旧 JS → 页面异常
  已有缓解: import('./pages/xxx.js?v=24060218') 使用版本号 query

建议: 构建时自动注入内容哈希到文件名 (Phase 11)
```

#### 6. 内存 — ECharts 实例未统一清理

```
backtest.js: 三 Tab 各一个 ECharts 实例
match-detail.js: 功守道弹窗 ECharts
gongshoudao.js: 多个雷达图/柱状图

问题: 页面切换时未调用 dispose() → 内存累积
建议: 统一 charts.js disposeAll() + 页面离开时调用
```

### 优化清单

| # | 改进项 | 文件 | 对应 Phase | 工作量 |
|---|--------|------|-----------|--------|
| 1 | api.js 错误码匹配 code≥0 | `api.js` | Phase 10 | 5 行 |
| 2 | 统一渲染状态处理 | `utils.js` (新增 `renderStates`) | Phase 19 | 30 行 |
| 3 | 提取数据转换模块 | `data-transform.js` (新建) | Phase 19 | 提取现有函数 |
| 4 | ECharts dispose 管理 | `charts.js` + 各页面 | Phase 19 | 20 行 |
| 5 | SWR 缓存策略 | `utils.js` (新增 `swrFetch`) | Phase 11 | 40 行 |
| 6 | 文件内容哈希 | 构建脚本 | Phase 11 | 脚本 |
| 7 | batch-match-odds 错误区分 | `pages/match-detail.js` 等 | Phase 1 + 10 | 各页面 ~10 行 |
| 8 | sessionStorage 缓存利用率 | 各页面 | Phase 19 | 检查是否用 getCache/setCache |

---

## 19. 预测模型与算法优化路线（Phase 14-18，新增）

### 19.1 当前预测体系现状

```
┌─────────────────────────────────────────────────────────────┐
│                    预测体系三支柱                              │
├───────────────┬──────────────────┬──────────────────────────┤
│ DeepSeek LLM  │ 功守道三模型融合   │ 动态权重引擎               │
│ (deepseek.js) │ (goal.js+fusion) │ (model-weights.js)       │
├───────────────┼──────────────────┼──────────────────────────┤
│ 文本五维分析    │ 公式驱动(A/B/C)   │ Softmax 命中率驱动         │
│ 无结构化输出    │ 非 ML 训练        │ 冷启动=等权                │
│ prompt 注入    │ 硬编码参数         │ 10分钟缓存                │
│ 数据: shuju+   │ 数据: recentGoal  │ 数据: prediction_log     │
│   JczqBasic+   │  +efficiency+     │  → actual_goals           │
│   功守道缓存     │  jiaoFenScores    │                           │
└───────────────┴──────────────────┴──────────────────────────┘
```

**核心瓶颈**：
- 三个模型都是**固定公式**，不随数据积累而改进（功守道的 model-weights 调的是融合权重而非模型本身）
- DeepSeek 输出是文本 JSON，结构化字段（confidence/spf方向/比分）不稳定，无法系统评估
- 没有**特征工程**：每一次新增数据源（JczqBasic/sporttery/500.fenxi）都是手动拼到 prompt 里，没有系统化的特征提取
- `prediction_outcomes` 表已建但**未使用**，回测只能用 `prediction_logs` 做基础统计

### 19.2 优化目标

| 维度 | 现状 | 目标 |
|------|------|------|
| **模型类型** | 固定公式 + LLM prompt | 公式 → ML 特征 → LLM 三层递进 |
| **特征工程** | 手动拼接字符串 | 结构化特征向量（~30 维） |
| **模型迭代** | 手动改公式参数 | 自动训练 + 版本管理 |
| **实验追踪** | 无 | prompt 变更 + 参数变更 + 结果对比 |
| **评估体系** | 命中率 / Brier | + 校准曲线 / 夏普比率 / ROI |
| **数据利用** | 只用近期数据 | 全量历史数据训练 + 时间序列 CV |

### 19.3 分阶段方案

#### Phase 14: 特征工程管道（3 天）

**目标**：将所有分散的数据源统一为结构化特征向量，任何新模型可直接消费。

**特征来源映射**（基于现有数据）：

| 特征类别 | 维数 | 数据来源 | 当前状态 |
|----------|------|----------|----------|
| **近期战绩** | 8 | `shuju_data/` (500.com) | ✅ 有数据，手动拼 prompt |
| **攻防指数** | 6 | `JczqBasic` (homePower/guestPower/进攻效率/防守效率) | ✅ 有数据 |
| **赔率信号** | 8 | `odds_history_v2` (初盘/终盘/离散度/亚指/大小球) | ⚠️ 有数据但未做特征化 |
| **交锋历史** | 4 | `h2h_history` 表 | ⚠️ 有表极少数据 |
| **功守道量化** | 6 | `cache.json` (xgHome/xgAway/ladderLabel/stability) | ✅ 已量化 |
| **市场信号** | 4 | `JczqChange` (支持率/热度/北单SP/赔率变更方向) | ✅ 有数据 |
| **积分榜** | 4 | `league_standings` 表 | ⚠️ 有表极少数据 |

**新增模块 `server/core/feature-builder.js`**：

```js
/**
 * 特征构建器 — 从多数据源提取标准化特征向量
 * 输出: { features: [v1,v2,...], labels: ['f1','f2',...], meta: {...} }
 */
function buildFeatures(matchNum, dateStr) {
  var f = {};

  // 1) 近期战绩 (8 维)
  var shuju = loadShujuStats(matchNum, dateStr);
  if (shuju) {
    f.home_last6_winRate = shuju.home.last6.winRate || 0.5;
    f.away_last6_winRate = shuju.away.last6.winRate || 0.5;
    f.home_last6_avgGoals = shuju.home.last6.avgGoals || 1.5;
    f.away_last6_avgGoals = shuju.away.last6.avgGoals || 1.5;
    f.home_last10_overRate = shuju.home.last10.overRate || 0.5;
    f.away_last10_overRate = shuju.away.last10.overRate || 0.5;
    f.home_last10_handicapRate = shuju.home.last10.handicapRate || 0.5;
    f.away_last10_handicapRate = shuju.away.last10.handicapRate || 0.5;
  }

  // 2) 赔率信号 (8 维)
  var odds = loadOddsSignal(matchNum, dateStr);
  if (odds) {
    f.spf_init_home = odds.spf.home || 2.5;
    f.spf_last_home = odds.spf_last.home || 2.5;
    f.spf_change_direction = (f.spf_last_home < f.spf_init_home) ? -1 : 1;
    f.handicap = odds.handicap || 0;
    f.dxq_line = odds.dxqLastPan || 2.5;
    f.discrete_diff = odds.discreteDiff || 0;
    f.support_rate_home = odds.supportRate || 0.5;
    f.odds_stability = odds.stability || 0.5;
  }

  // 3) 功守道量化 (6 维)
  var gs = loadGSCache(matchNum, dateStr);
  if (gs) {
    f.gs_xgHome = gs.xgHome || 1.2;
    f.gs_xgAway = gs.xgAway || 0.8;
    f.gs_advantage = gs.totalAdvantage || 0;
    f.gs_totalExpect = gs.fusionFinalTotal || 2.5;
    f.gs_consensus_strength = consensusToNum(gs.fusionConsensusType);
    f.gs_stability = gs.stabilityOverall || 50;
  }

  // 4) 交锋历史 (4 维)
  var h2h = loadH2H(matchNum, dateStr);
  if (h2h) {
    f.h2h_totalGoalAvg = h2h.avgTotalGoals || 2.5;
    f.h2h_overRate = h2h.overRate || 0.5;
    f.h2h_handicapRate = h2h.handicapRate || 0.5;
    f.h2h_homeAdvantage = h2h.homeAdvantage || 0;
  }

  // 标准化为向量
  var featureOrder = [
    'home_last6_winRate','away_last6_winRate','home_last6_avgGoals','away_last6_avgGoals',
    'home_last10_overRate','away_last10_overRate','home_last10_handicapRate','away_last10_handicapRate',
    'spf_init_home','spf_last_home','spf_change_direction','handicap','dxq_line',
    'discrete_diff','support_rate_home','odds_stability',
    'gs_xgHome','gs_xgAway','gs_advantage','gs_totalExpect','gs_consensus_strength','gs_stability',
    'h2h_totalGoalAvg','h2h_overRate','h2h_handicapRate','h2h_homeAdvantage'
  ];

  var vector = featureOrder.map(function(k) { return f[k] != null ? f[k] : 0; });
  return {
    features: vector,
    labels: featureOrder,
    dimension: vector.length,
    missingCount: vector.filter(function(v) { return v === 0; }).length,
    meta: { matchNum: matchNum, date: dateStr, sourceCount: (shuju ? 1 : 0) + (odds ? 1 : 0) + (gs ? 1 : 0) + (h2h ? 1 : 0) }
  };
}
```

**存储方案**：特征向量写入 `feature_store` 表（已建但空置），每次计算后持久化。

---

#### Phase 15: 结构化预测记录 + prediction_outcomes 激活（2 天）

**目标**：让 DeepSeek 的每一条预测成为可量化、可追溯的结构化记录。

**当前问题**：DeepSeek 输出 JSON 存到 `ai_predictions` 表的 `content` 字段（文本），结构化字段（信心度/方向/比分）需要从文本中提取，不稳定。

**改造方案**：

```sql
-- 激活已有 prediction_outcomes 表，增加字段
ALTER TABLE prediction_outcomes ADD COLUMN prediction_type TEXT DEFAULT 'gs';     -- gs/deepseek/ensemble
ALTER TABLE prediction_outcomes ADD COLUMN prompt_version TEXT;                    -- DeepSeek prompt 版本号
ALTER TABLE prediction_outcomes ADD COLUMN feature_hash TEXT;                      -- 特征快照哈希
ALTER TABLE prediction_outcomes ADD COLUMN prediction_detail TEXT;                 -- 完整预测 JSON
```

**预测记录标准流程**：

```
比赛创建 → 特征构建(feature-builder) → 功守道计算 → DeepSeek 分析
                                              ↓
                                    prediction_outcomes 写入:
                                    - prediction_type = 'gs'
                                    - predicted_home_score / away_score
                                    - predicted_direction / confidence
                                    - feature_hash = SHA256(特征向量)

比赛结束 → 比分回填 → 自动计算:
                                    - actual_home_score / away_score
                                    - direction_hit / over_under_hit / score_hit
                                    - brier_score / log_loss
```

---

#### Phase 16: 实验追踪框架（2 天）

**目标**：DeepSeek prompt 变更、模型参数调整 → 自动记录 + 效果对比。

**新增表**：

```sql
CREATE TABLE IF NOT EXISTS experiment_runs (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    experiment_id TEXT NOT NULL,       -- 实验名称
    run_id TEXT NOT NULL,             -- 运行ID (时间戳+hash)
    model_type TEXT NOT NULL,          -- gs/deepseek/ensemble
    model_version TEXT NOT NULL,       -- v1.0/v1.1
    prompt_version TEXT,               -- p_v2.3
    params_json TEXT,                  -- {temperature:0.7, model:"deepseek-chat", ...}
    feature_config_json TEXT,          -- 使用的特征列表和版本
    start_date TEXT NOT NULL,
    end_date TEXT,
    total_predictions INTEGER DEFAULT 0,
    total_outcomes INTEGER DEFAULT 0,  -- 有赛果的比赛数
    metrics_json TEXT,                 -- {accuracy, brier, roi, sharpe, ...}
    status TEXT DEFAULT 'running',     -- running/completed/aborted
    created_at TEXT DEFAULT (datetime('now','localtime'))
);
CREATE INDEX IF NOT EXISTS idx_exp_id ON experiment_runs(experiment_id, run_id);
```

**与现有 experiment-tracking Skill 联动**：

| Skill 功能 | 数据来源 |
|-----------|---------|
| prompt 版本对比 | `experiment_runs.prompt_version` + `prediction_outcomes` |
| 模型参数对比 | `experiment_runs.params_json` |
| 命中率趋势 | `prediction_outcomes` direction_hit 按 experiment_id 聚合 |
| 回滚决策 | 对比两个 run_id 的 `metrics_json` |

---

#### Phase 17: 量化预测能力增强（4 天）

**目标**：从"固定公式 + LLM 文本"升级到"特征工程 + 轻量 ML + LLM 增强"。

##### 17.1 方向预测模型（替代手动方向判断）

```
输入: 特征向量 (26 维) → 轻量 ML (逻辑回归/GBDT)
                            ↓
输出: 胜/平/负 概率 (calibrated)
        ↓
下游: 合成方案 + 投注决策
```

**实现方式**（不过度设计，用 Node.js 原生方案）：
- **初版**：加权 Logistic 回归（纯 JS 实现，无需 Python 依赖）
- **进阶**：调用 Python 子进程跑 sklearn (Phase 6 统一调度器已支持)
- 特征权重可解释（贡献度可视化）

##### 17.2 大小球预测模型

```
输入: 特征向量 + 功守道 xG → Poisson 回归
                                ↓
输出: 各总进球数的概率分布 → 大/小球概率
```

##### 17.3 比分预测模型（替代人工 Dutch 组合）

```
输入: 功守道 xG + 特征 → 双 Poisson 分布
                           ↓
输出: 各比分概率 → TOP3 比分 + 置信度
```

##### 17.4 模型校准（Platt Scaling）

```
原始预测概率 → 校准器 (历史命中率校正) → 校准后概率
                                        ↓
                              Brier Score 从 0.25 → 目标 < 0.20
```

---

#### Phase 18: 回测增强 — Walk-Forward 框架（3 天）

**目标**：让模型评估从"单次回测"升级到"滚动窗口交叉验证"。

```
时间线:  |← 训练窗口 (30天) →|← 测试窗口 (3天) →|
Day 1-30  训练 → Day 31-33 预测 → 记录结果
Day 4-33  训练 → Day 34-36 预测 → 记录结果
...滚动...
```

**新增 API**：

```
POST /api { action: 'walk-forward-backtest', experiment_id, window_days: 30, test_days: 3 }
→ 返回: 各窗口 accuracy/Brier/ROI 曲线 + 最佳/最差窗口分析
```

**回测看板增强**（backtest.js 新增功能）：
- 时间序列滚动命中率曲线（已有 ECharts，增加 WF 模式）
- 特征重要性随时间变化的热力图
- 模型衰减检测（最近 N 天命中率 < 历史均值 × 0.8 → 告警）

---

### 19.4 预测优化路线与数据架构改造的关系

```
数据架构改造 (Phase 1-13):
  ├── Phase 2 (SQLite 单源)      ← 预测模型需要稳定读取特征
  ├── Phase 3 (竞彩数据激活)     ← 新增 30+ 特征维度
  ├── Phase 4 (数据新鲜度)       ← 过期数据=过时模型
  └── Phase 6 (爬虫统一)         ← 特征数据自动更新

预测优化 (Phase 14-18):
  ├── Phase 14 (特征管道)        ← 依赖 Phase 2 的 SQLite 单源
  ├── Phase 15 (结构化预测)      ← 利用 Phase 10 的错误码体系
  ├── Phase 16 (实验追踪)        ← 利用 Phase 9 备份(实验数据防丢)
  ├── Phase 17 (量化模型)        ← 依赖 Phase 14 特征管道
  └── Phase 18 (Walk-Forward)    ← 依赖 Phase 15 结构化预测记录
```

---

### 19.5 实施优先级（预测优化可独立推进）

| Phase | 工期 | 优先级 | 依赖数据架构 Phase |
|-------|------|--------|-------------------|
| **Phase 14** 特征管道 | 3 天 | 🔴 高 | Phase 2 (SQLite 单源) |
| **Phase 15** 结构化预测 | 2 天 | 🔴 高 | Phase 10 (错误码) |
| **Phase 16** 实验追踪 | 2 天 | 🟡 中 | 无强依赖 |
| **Phase 17** 量化模型 | 4 天 | 🟡 中 | Phase 14 + 15 |
| **Phase 18** Walk-Forward | 3 天 | 🟢 低 | Phase 15 + 16 |

---

## 17. 改造常见坑位预警（来自项目实际踩坑历史）

| 编号 | 踩坑教训 | 影响 Phase | 预防措施 |
|------|---------|-----------|---------|
| 1 | **绕过 database.js 适配器直接操作 raw 实例** → 数据不落盘 | Phase 2, 8 | 所有新模块必须通过 `getAdapter()` 读写 |
| 2 | **新表只在业务模块建表，不加入 NEW_TABLES_DDL** → 切换后端时丢失 | Phase 2, 3 | 新表统一在 `database.js NEW_TABLES_DDL` 声明 |
| 3 | **新增 require() 依赖不检查部署范围** → 生产 Cannot find module | Phase 2, 9 | `deploy_gs.bat` 默认不含 `core/`，需手动添加 |
| 4 | **deploy.py 只传 JS，不传 Python 爬虫** → 生产爬虫版本不一致 | Phase 6 | `scraper_registry.json` 自动追踪脚本路径 |
| 5 | **Nginx /assets/ URL 映射到 miniprogram/images/ 而非 preview/assets/** → 资源 404 | Phase 5, 11 | 优先 emoji 内联字符，文件资源放在正确目录 |
| 6 | **better-sqlite3 本地跑通 ≠ sql.js 生产也能跑** | Phase 8 | 每个改动在两个后端都验证 |
| 7 | **WAL 模式下 PM2 kill 会丢未 checkpoint 的事务** | Phase 8 | synchronous=FULL + 定期 wal_checkpoint |
| 8 | **新增页面不同步 main-fusion.js 路由 → 空白页** | Phase 5 | 部署时检查 `_ensurePage` + `switchTab` 分支 |
| 9 | **定时任务相同错误无限重试无熔断** → 2026/5/21~23 fetchMatches 连续失败 390 次 | Phase 6 | scheduler 熔断: 3次退避30min, 10次熔断2h+告警 |
| 10 | **绕过适配器直接操作 raw.prepare()** → 2026/5/25~26 backfill 全线崩溃 70 次 | Phase 8 | 强制 `getAdapter().execRun()` |
| 11 | **部署漏传模块导致 dataModule is not defined** → 2026/5/31~6/2 API 全线报错 90 次 | Phase 10 | 启动时预检查 + 错误码区分系统故障 |
| 12 | **比赛结束后仍高频无意义轮询** → 2026/5/25 赛后 2h 仍每 20min 同步 | Phase 4 | 赛中 2s / 赛后 5min / 无比赛 30min |

---

## 18. 改造完成确认单（每个 Phase 部署后填写）

```
Phase: ___  部署日期: ___  操作人: ___

本地验证:
├── [ ] better-sqlite3 路径: npm test 通过
├── [ ] sql.js 路径: 临时切换后 npm test 通过
└── [ ] 新增/修改 API: curl 返回码正确

生产验证:
├── [ ] pm2 list: jc-zjfa + jc-sync online, restart=0
├── [ ] pm2 logs --lines 20: 无 Error/Cannot find module
├── [ ] python _verify_api.py: 4 层全部通过
├── [ ] 浏览器核心页面: 方案页 / 功守道 / PK / 回测 正常
├── [ ] 数据库大小: du -sh /root/server/midou_data.db (记录: _____)
├── [ ] 观察 30 分钟: PM2 内存稳定, API 延迟 < 200ms

回滚准备:
├── [ ] 备份路径: /root/backups/backup_*_pre_deploy/
├── [ ] 回滚命令已就绪: (记录具体命令)
└── [ ] 确认可回滚: ___ (是/否)
```

---

*本文档为 JC-ZJFA 数据架构改造的完整规划。核心原则：数据安全第一、本地充分测试、渐进式部署、随时可回滚。*

---

## 20. 开发就绪清单（进入开发前必读）

### 20.1 Phase 0: 开发环境准备（今天即可开始，0.5 天）

```
开发环境验证:
├── [ ] Node.js ≥ 16 (node -v)
├── [ ] Python ≥ 3.9 + pip (python --version, pip --version)
├── [ ] npm install (确认 better-sqlite3 编译成功)
├── [ ] npm test (确认 P0 测试全部通过)
├── [ ] 验证双后端: 
│     ├── 默认: node -e "const db=require('./server/database');setTimeout(()=>console.log(db.isAvailable()),2000)"
│     └── sql.js: 临时注释 server/database.js 第 1203 行 require.resolve('better-sqlite3') 再跑同上
├── [ ] 本地启动: node server/index.js → curl :3000/api (确认返回 JSON)
├── [ ] Git 状态: git stash / git status → 干净工作区
└── [ ] 分支: git checkout -b refactor/phase8-atomic-write (从 master 切出)
```

### 20.2 Phase 依赖关系矩阵（决定执行顺序）

```
可以立即开始（不依赖任何 Phase）:
├── Phase 8  ✅ 原子写入       (只改 database.js + 新建 file-utils.js)
├── Phase 9  ✅ 备份统一       (新建 backup.js，不影响现有流程)
├── Phase 7  ✅ Skill 文档     (只改 .md 文件)
└── Phase 16 ✅ 实验追踪       (新增表+模块，纯增量)

需要 Phase 8 完成后:
└── Phase 1  → 依赖 Phase 8 (改了 database.js 后才能安全改 cache.js)

需要 Phase 1 完成后:
└── Phase 2  → 依赖 Phase 1 (API 回路打通后再改存储)
    └── Phase 4  → 依赖 Phase 2 (有 odds_wide 表才能做新鲜度巡检)
        └── Phase 3  → 依赖 Phase 2 (统一表结构后再接入竞彩数据)

需要 Phase 2+3 完成后:
├── Phase 5  → 依赖 Phase 3 (有数据才能做前端展示)
├── Phase 14 → 依赖 Phase 2 (SQLite 单源才能做特征提取)
    └── Phase 15 → 依赖 Phase 14
        └── Phase 17,18

独立/低依赖:
├── Phase 6  → 可与 Phase 3 并行 (爬虫注册表是纯配置)
├── Phase 10 → 可与 Phase 1 并行 (仅改返回码，不碰数据)
├── Phase 11 → 独立 (前端缓存+WS)
├── Phase 12 → 独立 (监控端点)
└── Phase 13 → 独立 (安全加固)
```

### 20.3 每 Phase 具体文件改动清单

| Phase | 涉及文件 | 改动类型 | 行数估计 |
|-------|---------|---------|---------|
| **P8** | `server/database.js` (sql.js 分支 ~222 行) | 修改 `_saveToFile` | ~15 行 |
| | `server/database.js` (better-sqlite3 分支 ~333 行) | 调整 pragma | ~5 行 |
| | `server/database.js` (两分支 initDatabase) | 新增 `_checkIntegrity` | ~15 行 |
| | `server/core/file-utils.js` | **新建** | ~25 行 |
| | 7 个 JSON 写入脚本 | 引用改为 `file-utils.js` | 每文件 ~3 行 |
| **P1** | `server/index.js` (~5496 行 case 'batch-match-odds') | 新增 SQLite 回退块 | ~40 行 |
| | `server/core/cache.js` (~90 行 getOddsHistory) | 新增 DB 回退 | ~35 行 |
| **P2** | `server/database.js` (NEW_TABLES_DDL) | 新增 odds_wide DDL | ~15 行 |
| | `server/core/cache.js` | 新增 `getOddsUnified` | ~50 行 |
| | `server/data_sync.js` (`sync500Odds`) | 新增双写 | ~30 行 |
| | `server/index.js` (match-odds + batch-match-odds) | 切换读取源 | ~10 行 |
| **P9** | `server/core/backup.js` | **新建** | ~150 行 |
| | `deploy.py` (Phase 1 备份) | 改为调用 backup.js | ~10 行 |
| | 旧备份脚本归档 | 移到 `scripts/archive/` | — |

### 20.4 立即可做的 5 件事（今天，不等任何 Phase）

```
1. 环境准备 (Phase 0):
   cd e:/JC-ZJFA
   npm install
   npm test
   # 确认双后端都能跑

2. 新建分支:
   git checkout -b refactor/phase8-atomic-write

3. Phase 8 本地编写 + 自测:
   改动 database.js + 新建 file-utils.js
   → 本地切换 sql.js 路径验证 10 次写入
   → 验证 integrity_check

4. Phase 9 备份模块编写:
   新建 server/core/backup.js
   → 本地 node -e "require('./server/core/backup').backup('test')"
   → 确认生成了备份文件

5. Phase 7 Skill 文档更新:
   更新 .codebuddy/skills/data-pipeline/SKILL.md
   → 加入爬虫注册规范
```

### 20.5 Git 分支策略

```
master (生产)
  └── refactor/phase8-atomic-write     ← Week 1
  └── refactor/phase9-backup           ← Week 2 (可并行)
  └── refactor/phase1-odds-fallback    ← Week 1 (Phase 8 合并后)
  └── refactor/phase2-sqlite-primary   ← Week 3 (Phase 1 合并后)
  └── ...

合并规则:
├── 每个 Phase 一个分支
├── 本地测试通过 → PR → 代码审查
├── 生产部署验证 30 分钟 → merge 回 master
├── 下一 Phase 从最新的 master 切出
└── 独立 Phase (7/11/12/13) 可并行开发
```

### 20.6 本地测试数据准备

```powershell
# 从生产拉测试数据到本地 (不变更生产)
python deploy.py --download midou_data.db  # 或手动 scp
# 放到 server/midou_data_test.db (不改正式库)

# 本地测试环境变量 (Windows PowerShell):
$env:MIDOU_MOBILE="13570060818"
$env:MIDOU_PASSWORD="xxx"
$env:DEEPSEEK_API_KEY="sk-xxx"
$env:NODE_ENV="development"

# 测试数据库切换: 
# 临时修改 database.js DB_PATH 指向 midou_data_test.db
```

### 20.7 开发日报模板（每天 5 分钟记录）

```
日期: ____  当前 Phase: ____

今日完成:
├── 文件: _____  改动: _____ (行数)
└── 文件: _____  改动: _____

测试结果:
├── [ ] better-sqlite3 路径通过
├── [ ] sql.js 路径通过
└── [ ] npm test 通过

遇到的问题:
└── _____ (解决方案: _____)

明日计划:
└── _____
```

### 20.8 文档自身待完善项（开发过程中逐步细化）

| 待完善项 | 何时完善 | 负责人 |
|---------|---------|--------|
| Phase 1 `batch-match-odds` 具体行号确认 | P1 开始前 | 开发者 |
| Phase 2 `odds_wide` 与 `odds_history_v2` 的精确映射关系 | P2 开始前 | 开发者 |
| Phase 3 bridge 桥接脚本的 1022xxx→周五xxx 映射算法 | P3 开始前 | 开发者 |
| Phase 6 `scraper_registry.json` 完整条目 (14 个爬虫) | P6 开始前 | 开发者 |
| Phase 17 逻辑回归 JS 实现的训练参数 | P17 开始前 | 算法工程师 |
| 各 Phase 的 `npm run test:p0` 预期新增测试用例 | 每个 Phase 完成后 | 开发者 |

---

*开发就绪清单结束。建议从 Phase 0 环境准备开始，Phase 8 作为第一个开发任务。*

---

## 21. 系统日志分析 — 数据相关问题与优化建议

> 数据来源: `logs/error.log` (5/21~6/3) + `logs/data_sync.log` (5/25)

### 21.1 错误日志统计

| 错误类型 | 首次出现 | 最后出现 | 频次 | 影响 |
|---------|---------|---------|------|------|
| `fetchMatches is not a function` | 5/21 12:37 | 5/23 23:27 | **~390 次** | 定时爬取全线瘫痪，每 5 分钟重试失败 3 天 |
| `db.prepare is not a function` | 5/25 19:47 | 5/26 19:18 | **~70 次** | 回填模块绕过适配器，数据无法写入 SQLite |
| `dataModule is not defined` | 5/31 17:05 | 6/2 17:01 | **~90 次** | API 某 handler 引用未加载模块，前端持续报错 |
| `主备账户均无法登录` | 5/31 17:04 | 6/2 19:32 | **~20 次** | 数据源认证失效，match-list 全线不可用 |
| `ENOTFOUND midou310.com` | 5/22 10:52 | 5/25 23:29 | ~10 次 | DNS 不可达，赛事+推荐数据缺失 |
| `Maximum call stack size exceeded` | 5/28 19:29 | 5/28 19:29 | 5 次 | AI 预测模块死循环 |
| `Cannot read properties of null` | 6/2 17:49 | — | 1 次 | batch-match-odds null 处理缺失 |

### 21.2 日志中暴露的 5 个系统性问题

#### 问题 1：错误重试失控,无熔断机制 🔴

```
5/21 12:37 → 5/23 23:27
连续 59 小时，每 5 分钟重试一次 fetchMatches()
总重试: ~390 次，全部失败且错误完全一致
结果: 日志膨胀 + CPU 空转 + 问题被掩盖
```

**建议**：在 `scheduler_v2.js` 中增加**指数退避 + 熔断**:
- 相同错误连续 3 次 → 退避 30 分钟
- 相同错误连续 10 次 → 熔断 2 小时 + 告警
- **对应 Phase 6 (爬虫统一编排) 的 `retry.backoff: exponential` 配置**

#### 问题 2：绕过适配器的数据库操作 🔴

```
5/25 19:47 → 5/26 19:18
[backfill] 回填异常: db.prepare is not a function
backfill 模块直接调用 raw db.prepare() 而非 database.getAdapter().execRun()
```

这正是 AGENTS.md §7.6 记录的教训——**在 sql.js 后端下 raw 实例的 prepare 与 better-sqlite3 不兼容**。**对应 Phase 8 修复范围**。

#### 问题 3：API 引用未加载模块导致全线报错 🔴

```
5/31 17:05 → 6/2 17:01
dataModule is not defined — 每次 API 调用都报错
持续 2 天，前端用户看到的是统一的 "请求失败" 提示（因为错误码全为 code:0）
```

**建议**：
- 短期：sever/index.js 启动时预检查所有模块引用（try-require 并记录）
- 长期：**对应 Phase 10 错误码体系**，这类错误应返回 `code:-2` 让前端区分"数据为空"和"系统故障"

#### 问题 4：data_sync 重复重启 + 赛外时段无效轮询 🟡

```
5/25 08:29 data_sync 启动
5/25 11:46 data_sync 再次启动 (PM2 restart?)
5/25 21:08 所有比赛结束
5/25 21:09 → 23:26 仍然每 20 分钟同步一次 "今日无比赛" (无意义轮询)
```

**建议**：
- **比赛结束后自动降频**：赛中 2s → 赛后 5min → 无比赛 30min
- **对应 Phase 4 数据质量自动化的 `data_sync` 周期性检查优化**

#### 问题 5：500odds 文件存在即跳过，不验证内容有效性 🟡

```
5/25 08:29 [500odds] 2026-05-25 已有数据，跳过
5/25 11:47 [500odds] 2026-05-25 已有数据，跳过
```

文件存在 ≠ 数据正确。如果上次抓取失败生成空文件、或比赛编号已变更，这个跳过逻辑会导致赔率数据过期。**对应 Phase 2 的 `needsRefresh()` 内容感知刷新**。

### 21.3 日志管理优化建议

| 问题 | 现状 | 建议 | 对应 Phase |
|------|------|------|-----------|
| error.log 无轮转 | 单文件累积 | `winston-daily-rotate-file` | Phase 13 |
| combined.log 1.5MB | 持续增长 | 按天切割 + 30天清理 | Phase 13 |
| data_sync.log 含大量 live_score 轮询 | 每 2 秒一条 | 赛中 2s / 赛后 5min / 无比赛 30min | Phase 4 |
| json.log 1MB | 持续增长 | 按天切割 | Phase 13 |

### 21.4 优先级最高的 3 条立即优化项

| # | 优化项 | 解决 Phase | 预计受益 |
|---|--------|-----------|---------|
| 1 | scheduler 熔断 + 退避 | Phase 6 / 立即可改 | 消除重复 390 次的无效重试 |
| 2 | 所有 DB 操作强制过适配器 | Phase 8 | 消除 `db.prepare is not a function` |
| 3 | 500odds 内容感知刷新 | Phase 2 | 消除"已有数据跳过但数据过期" |
